# Chronological simulation and SEC provenance wave

Subsequent [SEC access resolution](../catalyst-provenance/access-resolution.md)
uses a genuine monitored contact and a corrected real-index adapter. The tested
archive path now returns HTTP 200; the initial refusal below remains dated evidence.

This wave advances the accepted research stack from fixed stress scenarios to
chronological selection discipline and native filing-data provenance. The user
selected historical simulation first, asked to resolve SEC integration using
[EdgarTools](https://github.com/dgunning/edgartools), and retained the dedicated
paper account as a later lane. The [plan](plan.json) records the bounded scope.

Two independent workers own isolated changes. The evaluation worker uses the
existing LEAN sample data and a separately pinned upstream chronological splitter.
The data worker checks current related starred repositories and EdgarTools'
supported SEC APIs, then records actual acquisition or refusal. A third worker
reviews causality, integrity, scope and claims. The coordinator alone integrates
and publishes the result.

The evaluation is a curated SPY/QQQ/IWM control. It does not reconstruct the
historical all-equities universe, delisted names, +200% movers or catalyst arrival
times. The 2021 segment is reserved by this experiment; once evaluated it is
inspected evidence for future work. Price-label results and native engine cash
accounting are distinct evidence classes.

SEC filing acceptance, source publication and our first observation are distinct
clocks. A filing downloaded today does not become information that this system
had in a past trading session. Current ticker mappings and revised indexes do
not repair that limitation. The contact header identifies the real research
project; library configuration acceptance does not prove SEC accessibility.

The [passwordless dashboard](http://127.0.0.1:13000/d/research-grand?refresh=30s)
shows the active plan, recorded worker checkpoints and data/evaluation gates.
Those checkpoints are explicitly separate from native process or workflow
observations. Existing service health, model usage and fixed stress results stay
available. No model token-savings percentage is inferred from this research.

## Chronological evaluation result

The [native chronological study](../research-evaluation/README.md) ran upstream
skfolio 1.2.9 over pinned LEAN sample inputs. It produced **20 development folds,
21 frozen selections and 6,605 candidate records**. The training-selected rule's
reserved mean five-session price label after a fixed cost proxy was **0.118407%**,
below the equal-weight control's **0.162293%**. All alternatives remain visible;
the better unselected outcomes did not trigger retuning.

This is raw-price label evaluation. It did not execute the LEAN engine again,
model cash/distributions/fills, or establish attainable returns. The previous
native LEAN stress receipts retain their own separate engine/accounting scope.
The [evaluation receipt](../research-evaluation/receipt.json) gives exact decimals,
the frozen plan, all native commands and reviewed artifact hashes.

## SEC and repository review result

[EdgarTools 5.58.0](../catalyst-provenance/README.md) is installed in a separate
42-package version-pinned environment. Its native parser processed a pinned real
8-K HTML fixture into Markdown. A missing acceptance timestamp keeps that document
quarantined for historical trading decisions. Two synthetic index members separately
verify amendment filtering and bounded native context output.

The first attempted request failed locally because of an empty proxy setting;
after removing only empty proxy values in the isolated process, one genuine native
SEC metadata request returned **HTTP 403**. Retries were disabled and the network
branch stopped. No fresh SEC dataset was accepted. The truthful project contact
is configured locally for both native identity variables; the SDK's recommended
email contact was not invented. The [receipt](../catalyst-provenance/receipt.json)
retains these distinctions and the un-hashed-wheel dependency-lock limitation.

The [source review](../../../catalogs/us-equities/simulation-data-review.md) covers
12 bounded decisions. A fresh identity audit found **342 public stars**, including
five additions. The combined index now has **499 repository identities** with
**1,012 typed source pointers**; a catalog entry is not an installed capability.

The integrated local SDK suite passed **228 tests with zero skips**. Independent
reviews accepted both worker changes and the dashboard's active-plan/star routing.
Native dashboard acceptance passed **10 queries and 3 generation checks**. Exact
observation and publication checks accompany [this wave's receipt](receipt.json).

The stop condition is reviewed, reproducible evidence for this bounded wave with
remaining data/host/account gates stated; it is not a universal SOTA ranking or
strategy promotion. Source pins and locked adoption commands travel with each
receipt; a new host still performs its own native acceptance.
