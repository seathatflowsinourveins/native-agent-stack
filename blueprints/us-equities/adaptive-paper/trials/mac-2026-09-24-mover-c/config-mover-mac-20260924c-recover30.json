{
  "schema_version": 1,
  "mode": "mover_paper",
  "protocol": "mover-early-entry-v1-20260924",
  "endpoint": "https://paper-api.alpaca.markets",
  "feed": "sip",
  "regular_session_only": false,
  "extended_hours_enabled": true,
  "sessions": {
    "extended_hours": true,
    "overnight_holds": false,
    "overnight_gross_multiple": "1.0"
  },
  "catalyst_orders_enabled": false,
  "benchmarks": [
    "SPY"
  ],
  "capital_usd": "10000",
  "max_gross_exposure_usd": "2000",
  "max_order_notional_usd": "2000",
  "max_order_quantity": 100,
  "max_order_qty_mode": "notional",
  "max_held_symbols": 5,
  "max_outstanding_orders": 20,
  "max_gross_loss_usd": "1000",
  "max_drawdown_usd": "1000",
  "max_leverage": "1",
  "max_spread_bps": "100",
  "quote_max_age_seconds": 3,
  "max_api_requests_per_minute": 200,
  "max_submit_requests_per_minute": 180,
  "duration_seconds": 3600,
  "cleanup_seconds": 600,
  "min_entry_close_seconds": 600,
  "order_timeout_seconds": 10,
  "mover": {
    "rule": "10:00|G20|V250000|any",
    "session_scope": "any_session",
    "trial_end_et": "16:10",
    "exit": "X1",
    "entry": {
      "limit_cap_bps": "50",
      "timeout_seconds": 60,
      "window_seconds": 30
    },
    "exit_orders": {
      "limit_cap_bps": "50",
      "timeout_seconds": 10,
      "max_orders_per_symbol": 20
    },
    "flatten_reserve_seconds": 120,
    "max_scan_age_seconds": 300,
    "max_symbols": 5,
    "max_entry_notional_usd": "200",
    "appreciation_allowance": "2",
    "gross_guard_fraction": "0.9",
    "stream_quote_timeout_seconds": 30,
    "rung_schedule": [
      {
        "from_session": 1,
        "to_session": 20,
        "rung": "1"
      }
    ]
  },
  "notes": "Recovery-only copy of config-mover-mac-20260924c.json with mover.stream_quote_timeout_seconds 30 (the engine maximum), a recorded decision for a sell-only after-hours recovery: at 16:26 ET SRZN quoted 0 times in 30 s and PFSA/GRML had 15-16 s gaps, so the 3 s transport readiness could not start. The ledger's 3 s order gate still applies to every sell."
}
