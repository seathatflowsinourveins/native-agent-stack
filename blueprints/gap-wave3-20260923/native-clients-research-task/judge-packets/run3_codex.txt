Anthropic’s **prompt caching**, supported by the production coding agent Aider, reuses repeated prompt prefixes such as instructions and repository context. It reduces repeated input processing during tool calls and long sessions, rather than shortening the logical context. Anthropic’s published cache-read rate is **10% of standard input-token pricing**, a **90% cost reduction for cached input tokens**; uncached tokens and cache writes still incur separate charges.