# Cross-layer native acceptance — 20 September 2026

This wave qualifies concrete workflows on the observed host. It does not certify
every catalog entry or another machine. Source review, installation, acceptance
and deployment remain distinct.

| Layer | Observed native task | Scope and remaining boundary |
| --- | --- | --- |
| [Application delivery](application-delivery/README.md) | Locked Next16.3.5/React19.3.0 UI, FastAPI0.141.1, real PostgreSQL18.6,12 API checks, browser create/update and exact row after full restart | Mac synthetic project; no production/user authentication/cloud acceptance. Supported TypeScript5.9.3 retained after7.0.2 failed the native generator. |
| [Document ingestion](document-ingestion/README.md) | Current Poppler26.09.0,3 permitted pages,24 table cells,8 cited positive and4 absent queries; strict frozen-source guard | Designed born-digital fixture with explicit table geometry; OCR, arbitrary layout and semantic search remain unqualified. |
| [Container storage](container-storage/README.md) | Apple container1.4.1, pinned PostgreSQL18.6 image, native ext4 volume and row retained after stop/start | Mac Documents bind mount stalled; named volume passed. No crash/reboot or production durability claim. |
| [WSL recovery](wsl-restore/README.md) | Native restic0.19.1, two exact synthetic snapshot restores, wrong-password/corrupt-copy refusals, fresh SSH recovery | Same physical WSL host; independent off-host key-loss disaster recovery remains open. |

[WSL native quality tools](wsl-native-tools/README.md) add verified gitleaks8.30.1
and Worktrunk0.78.0 with clean/secret controls, dirty-worktree refusal, exact pin
binding and retained timeout diagnostics. Both native attempts and the original
runner remain available; local PATH promotion is separate deployment evidence.

## Context and runtime efficiency

Use the smallest sufficient evidence packet, then open its source when needed.
The document recipe returns at most three records of240 characters with document,
page, source hash and bounds; missing evidence stays empty. Native project locks
and generated API contracts reduce repeated environment and schema investigation.
The publication validator evaluates Git publication paths rather than installed
build trees, while retaining tracked and explicitly hash-listed files. Full native
logs, failed attempts and artifact hashes remain available outside routine context.

These are architectural constraints and observed artifact boundaries, not a
measurement of complete provider usage. Native model quality, caching, compaction
and explicit scoped memory remain intact. No model call is needed to replay the
deterministic artifact checks. Do not repeat accepted native work solely to refresh
a timestamp or load the entire catalog into every worker.

## Acceptance sequence

Read the selected recipe, its exact source pins, compatibility and license notes.
Use its isolated prefix/project and upstream commands. Preserve failed attempts;
verify the declared task, negative cases and cleanup. Record the actual host before
promoting a local tool or marking another profile ready. Keep rollback paths and
native account stores local.

The catalog is a selection aid. Stars and awesome lists establish discovery, not
installation permission, compatibility or successful execution. Shared services,
accounts, global configuration and publication have one coordinator owner.
