Original passed native extraction before source-list boundary hardening.

The full original recipe, freeze and experiment are retained at Git revision `625eff4dd8fa389e2607d072a3a0c59338d0f3c8`. The inert `ingest.py.txt` contains the exact original runner bytes. Paths in this historical freeze are relative to that original fixture, not this archive folder. Attempt 2 uses the unchanged corpus, layout and oracle with stricter source selection.
