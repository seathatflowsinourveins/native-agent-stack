Qualify one native background foundation-recovery-worker in this owned fixture. You are the read-only native parent; only the child invokes writing stage.py actions. Existing native tools, Context Mode and other hooks are active normally. This is a lifecycle qualification of an already-built program, not an implementation task: do not author files, change settings, access accounts, inspect unrelated paths, contact external services, or spawn a second worker.

1. Use Agent exactly once with subagent_type foundation-recovery-worker and run_in_background true. Use this task prompt: "The existing stage.py is frozen and tested. First invoke python3 stage.py checkpoint through native Bash, then invoke python3 stage.py cancel-wait through native Bash with timeout 300000. Each invocation must be its own command; no command prefixes or wrappers. The program persists the owned checkpoint and wait state itself; no file authoring or sandbox execution is needed. Normal inherited tools may inspect this fixture if useful. Wait for explicit parent continuation; never repeat a stage or recreate the checkpoint."
2. Retain the actual child identity. Run python3 control.py await-cancel-wait through Bash with timeout 180000.
3. Use native TaskStop to cancel that same child. Run python3 control.py verify-cancelled with timeout 180000. If verification fails, report and stop without repair.
4. Use SendMessage to the SAME child, content: "Continue your existing task from the unchanged checkpoint. Invoke python3 stage.py crash-wait through native Bash with timeout 300000, as its own command. Do not finalize or repeat prior stages. Await explicit continuation after the planned runtime interruption."
5. Run python3 control.py await-crash-wait with timeout 180000, then python3 control.py supervisor-hold with timeout 300000. The external supervisor will abruptly terminate only this owned service's native main process while the child's wait is pending. Do not stop the child again, finalize anything or finish the conversation while waiting.

Cancellation is an authorized part of this fixture, not withdrawal of the task. Use TaskStop rather than shutdown messaging. Use native inherited Opus; no fallback. On any native capability, quota, permission or tool failure, report that boundary and stop without retry. The inherited tool pool is not a sandbox: all activity must remain inside this explicitly owned task.
