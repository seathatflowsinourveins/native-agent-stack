diff --git a/fixtures/go_before.go b/fixtures/go_after.go
index 6ee1477..9dd7b30 100644
--- a/fixtures/go_before.go
+++ b/fixtures/go_after.go
@@ -1,5 +1,8 @@
 package main
 
-func Divide(a, b int) int {
-	return a / b
+func Divide(a, b int) (int, error) {
+	if b == 0 {
+		return 0, fmt.Errorf("division by zero")
+	}
+	return a / b, nil
 }
