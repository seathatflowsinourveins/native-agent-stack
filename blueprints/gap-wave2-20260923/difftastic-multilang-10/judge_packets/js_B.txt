You are reviewing a code diff blind, with no knowledge of which tool produced it.

Below is the ENTIRE output of a diff tool comparing a 'before' and 'after' version of a small javascript function. Do not assume anything about the code beyond what is shown.

--- DIFF START ---
diff --git a/fixtures/js_before.js b/fixtures/js_after.js
index fe3c63b..186bf26 100644
--- a/fixtures/js_before.js
+++ b/fixtures/js_after.js
@@ -3,5 +3,8 @@ function getDiscount(total, isMember) {
   if (total > 100) {
     discount = 10;
   }
+  if (isMember) {
+    discount += 5;
+  }
   return discount;
 }

--- DIFF END ---

Question: Does this diff clearly show the following change: Does the diff clearly show a new `if (isMember)` block adding 5 to discount, as an ADDITION rather than a modification of the existing `if (total > 100)` block?

Answer with EXACTLY this format, two lines:
ANSWER: YES or NO
EXPLANATION: one sentence, based only on the diff text shown above, describing what you can see changed.