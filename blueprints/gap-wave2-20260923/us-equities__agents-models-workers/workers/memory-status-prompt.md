Call the MCP tool memory_status from the ai-memory server exactly once, with no arguments.
Do not call any other tool. Reply with the tool's text output verbatim, or, if the call was
not possible, the exact error or reason.
