Call the MCP tool memory_status from the ai-memory server exactly once, with no arguments.
If the tool is deferred, you may use the tool-discovery/search tool to load it first. Do not
call any other tool. Reply with the memory_status text output verbatim, or, if the call was
not possible, the exact error or reason.
