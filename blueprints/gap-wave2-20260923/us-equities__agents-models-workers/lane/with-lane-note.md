
Tooling rule for this run: process any command output that may be large with the context-mode
MCP tool ctx_execute (run the computation inside it and return only the small result), rather
than printing large output into the conversation.
