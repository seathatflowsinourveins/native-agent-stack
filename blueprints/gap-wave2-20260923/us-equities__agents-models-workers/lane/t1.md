In the current directory, tests/ holds Python test files. Count every test function definition
(lines matching optional indentation, optional "async", then "def test_") across all .py files
under tests/ recursively, and find the single file with the most. Reply with ONLY one JSON object:
{"total_test_functions": <int>, "file_with_most": "<path relative to cwd>", "file_with_most_count": <int>}
