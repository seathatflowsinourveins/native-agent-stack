In the current directory, manifests/evidence.json has a "files" array of {path, sha256, bytes}.
Count the entries whose path starts with "evidence/receipts/" and sum their bytes. Reply with
ONLY one JSON object: {"receipt_entries": <int>, "receipt_bytes_total": <int>}
