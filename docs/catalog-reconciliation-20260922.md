# Catalog reconciliation with the live Claude owner — September 22

The live Claude owner reviewed published revision `9a88388` and returned a concrete
clarity finding: **nine of 20 coordinator labels differed from canonical layer
decisions**. All 20 summary labels now mirror the canonical catalogs, and the
handbook uses the same decisions. The selected 68 component pins and candidate
dispositions did not change.

The [preserved Claude return](../evidence/artifacts/catalog-reconciliation-20260922/claude-return.json)
records the peer's exact findings after private path/session substitutions.
The [coordinator adjudication](../evidence/artifacts/catalog-reconciliation-20260922/adjudication.json)
retains all nine previous labels, their corrections, provenance and qualifications.
A separate Codex reviewer independently confirmed the mismatches and the narrower
interpretation below.

## Authority and corrected labels

`layers[].decision` in [foundation](../catalogs/landscape/foundation.json) and
[north star](../catalogs/landscape/us-equities.json) is authoritative for layer
status. Each [synthesis row](../catalogs/landscape/blind-convergence.json) links its
canonical decision by file and JSON pointer. Candidate-level dispositions and
the destination host's own receipts govern installation and acceptance.
Historical peer judgments remain source evidence.

| Layer | Previous summary label | Canonical decision now displayed |
| --- | --- | --- |
| instructions-skills | retain_targeted_practice | adjust |
| workers | retain | keep_but_compare |
| code-navigation | retain_expand_discovery | retain |
| web-research | retain_complementary_tools | retain |
| token-efficiency | retain | keep_but_compare |
| quality-evaluation | retain_expand_discovery | retain |
| hosting-services | retain_complementary_tools | retain |
| observation-inference | retain_complementary_tools | keep_but_compare |
| agents-operations | retain_keep_comparisons | retain |

## Findings accepted and qualified

The peer called this an unsupported promotion. The supported defect is label
ambiguity: `retain` could look settled where the canonical decision was
`keep_but_compare`. Existing scope guards and candidate dispositions already
preserved the comparison gates; no source-only candidate acquired runtime
acceptance or a selected pin through these labels.

The returned artifact combines an extractor's checks and a critic's judgments.
The extractor checked Git and queried ledger fields. The critic inherited that
extraction and did not independently rerun those commands. Its later finding
reconciles the extractor's star-pool concern. These are stage-specific limits,
not contradictory claims about one uniform execution.

The return's broad assertion that no execution exists is narrowed to this
review's own activity. Earlier native tasks, memory lifecycle probes and scoped
comparisons retain their exact evidential scope. They do not prove universal
quality leadership. Its complaint about missing machine-readable WF3 differences
is also superseded by the already published coverage JSON and frozen screening
ledger. The audit reproduces the 75/62/75/125 coverage counts and joins; the earlier
independent top-12 star arithmetic is separate from that script.

A newer release alone does not invalidate an accepted version. Likewise,
otel-tui's upstream default listener is distinct from the accepted local profile's
explicit loopback setting. No additional install or pin change follows from these
source observations.

## Verification and cooperation boundary

The existing report audit now also checks all 20 canonical status joins, exact
decision pointers and handbook statuses. Negative fixtures reject an unqualified
`retain` for an open worker comparison, a wrong pointer, a missing layer and a
misleading handbook label. Historical peer wording may differ without becoming
operative authority. The original 514-row screening ledger is unchanged.

The follow-up used the live owner's native two-stage review: source-scout
(Sonnet/medium), then evidence-reviewer (Opus/high). Its returned summary attributes
129,553 subagent tokens and 26 tool uses to that workflow; those are reported
workflow counters, not provider-billed totals or savings. This post-seal review
also retains the [per-child requested/resolved models, effort and usage](../evidence/artifacts/catalog-reconciliation-20260922/claude-usage.json),
with both children complete. Those provider-returned counters remain separate
from the workflow summary and do not establish savings. The review
read the published synthesis and **was not blinded**. Earlier residual name
exposure remains disclosed; agreement is not proof of zero bias.

The Codex example's three-thread limit and Claude's eight-worker workflow cap
remain separate settings. Only misleading explanatory text was corrected.
The user-owned live Claude session was not restarted or stopped.

The dated catalog is ready to guide the next-PC setup. Runtime cancellation and
replay comparisons, representative memory quality, target-hardware inference,
broker-specific paper acceptance and the new PC's installation/recovery checks
remain explicit gates. Use the [grand handbook](grand-catalog-handbook.md) and
the [20-layer findings](blind-catalog-layer-findings-20260921.md).
