# Full-stack native practice and returned evidence

The subsequent [landscape reconciliation](../catalogs/landscape/README.md) covers
all 24 foundation/domain layers, expands the joined research index to 525
identities, and records the current 343-public-star snapshot. The original wave
counts and measurements below retain their dated scope; the current HTML derives
its totals from the canonical records.

The September 21 convergence connects **68 selected components**, **16 foundation
layers** and **513 research repository identities** to upstream commands, dated
native results, dashboards and portable recipes. Open the [offline HTML](ecosystem/index.html),
select a component, and use its attached-results button. Screenshot bytes and
returned output are embedded. Missing coverage stays visible; inclusion does not
mean installation or a passed run.

The [wave receipt](../evidence/receipts/full-stack-convergence-20260921.json) links
public artifacts. The [component coverage](../evidence/artifacts/full-stack-convergence-20260921/component-coverage.json)
joins every selected pin, command, recipe and registered receipt. Complete private
native outputs and seven separate counter scopes live in the host's report. Use
the [portable reporter](../tools/token-report/README.md) for another PC; historical
acceptance never becomes that PC's passed status.

## Current native results

Both clients completed the same five-step read-only task: RTK Git output, Serena
source/callers, scoped QMD BM25 search/get, scoped ai-memory query, and Context
Mode file extraction/stats. Each returned eleven tool results including discovery,
with no tool-result failures. Independent review inspected original streams,
native usage, source and unchanged project-status bytes. Hooks and caches still
wrote their scoped state; a claim of no memory mutation would be incorrect.

| Client | Observed model | Consumed tokens | Native accounting |
| --- | --- | ---: | --- |
| Codex 0.155.1 | GPT-6 Astra, ultra | 255,335 | 251,225 input + 4,110 output; cache/reasoning are subsets |
| Claude Code 2.1.278 | Fable 5.1 | 250,492 | 100 ordinary input + 45,916 cache creation + 199,027 cache reads + 5,449 output |

These tasks have no matched baseline. The older fixed-order comparison remains
separate: Codex **135,217 → 119,998** (11.26% lower); Claude **292,561 → 368,121**
(25.83% higher). Both older tasks passed, but cache state and full plugin absence
were uncontrolled. See [returned client evidence](../evidence/artifacts/full-stack-convergence-20260921/native-client-results.json)
and [independent review](../evidence/artifacts/full-stack-convergence-20260921/independent-review.md).

## All foundation layers

| Layer | Selected native practice | Acceptance / continuation |
| --- | --- | --- |
| Native clients | Codex and Claude native accounts and streaming output | Current returned tasks; preserve independent accounts/defaults |
| Instructions/skills | Native discovery and selected upstream skills on demand | Keep catalogs out of always-loaded instructions |
| Workers | Bounded child, owned writing worktree, integrating coordinator | Historical cancellation, continuation and parent-crash recovery |
| Isolation | Native permissions and sandbox-runtime | Qualify actual filesystem/network boundaries |
| Code navigation | Serena, ast-grep, scoped jCodeMunch/codebase-memory | Original source and references, not compressed structure alone |
| Document retrieval | Scoped QMD BM25 search then get | Current useful hits; zero vectors is intentional |
| Semantic RAG | SocratiCode → Nemotron → Qdrant | Real embedding plus scoped collection health |
| Durable memory | ai-memory scoped capture/read/write/backup | Current query and compatible MiniLM; historical restore |
| Web research | Tavily, Context Hub, HF, OpenResearch | Source pins and useful returned content; retain refusals |
| Token efficiency | RTK, Context Mode, guarded Headroom, measured TOON | Separate native estimates, artifact counts and provider trials |
| Quality evaluation | Upstream tests and independent observations | Separate vendor tests from our fixtures/integration checks |
| CI/supply chain | Native GitHub CI, gitleaks, zizmor, Syft, integrity | CI validates public artifacts; no inferred GPU/provider acceptance |
| Scheduling/supervision | Dagu/systemd execution, retry and cancellation | Dated native histories and selected guest reboot |
| Hosting/services | Loopback services and explicit state roots | Populated APIs/UI; production/cloud scope stays separate |
| Recovery/portability | Versioned prefixes, rollback, Restic native restore | Scoped disposable/hosted-job evidence; qualify new PCs |
| Observation/inference | Grafana/Prometheus/Loki/OTel and native outputs | Source time differs from scrape time; usage differs from savings |

The [foundation catalog](../catalogs/foundation/manifest.json) retains 47 capability
decisions. The [lifecycle guide](../adoption/lifecycle.md) covers installation,
restart, recovery and cleanup. Reuse accepted evidence while its inputs and scope
match; additional testing should resolve a concrete uncertainty.

Two native Claude terminal resumes in this wave did not reach the actual HUD. Existing
authentication and onboarding were ready; the effort overlay remained visible
after Enter was sent. Settings/model/effort stayed unchanged and owned processes
were cleaned up. The [attempt record](../evidence/artifacts/full-stack-convergence-20260921/hud-observation.json)
preserves those attempts. The later [foundation-closure observation](foundation-closure-20260921.md#actual-session-and-terminal-evidence)
records actual HUD rendering; the current capability/lifecycle catalog now joins
that evidence, with the original screen-observation and statistics limits.
Outer bridge, gateway and Beads recovery qualification also remain optional gaps.

## Useful upstream dashboards

[Exact dashboard observations](../evidence/artifacts/full-stack-convergence-20260921/dashboard-results.json)
include ten inspected screenshots and native/browser commands. Loopback URLs
identify the source host, not a service on every PC.

- ai-memory: native project counts, a ten-hit FTS search and an opened article.
  Public captures exclude page bodies/session titles. MiniLM384 has no missing
  latest-page embeddings in this wave's capture; LLM consolidation was disabled
  at that capture time. The subsequently merged [native consolidation
  qualification](native-memory-consolidation.md) enables the existing native
  provider and verifies persistence/readback for actual Claude and Codex sessions.
- Qdrant: five green collections; 225 points in the selected code collection.
- SocratiCode: upstream generated snapshot with 29 files, five edges, 266 symbols
  and 323 calls. It is not a live graph refresh.
- Grafana: official UI with locally provisioned panels, source dates and seven
  healthy Prometheus targets. A fresh scrape does not make stale source data fresh.
- Dagu: Last 30 days shows nine retained runs, six successes, two failures and
  one abort. Today is empty. No new jobs were created to decorate the dashboard.
- AgentsView: selected archive aggregates; session titles and bodies stay private.
- OmniRoute: 57.7K retained tokens across 31 gateway requests are usage. Token
  Saver is off with zero savings. Native clients keep their independent routes.

The [later dashboard resolution](dashboard-gap-resolution.md) adds current native
memory consolidation, archive refresh, passwordless gateway dashboard access,
real Claude telemetry, corrected ntfy templates and NVIDIA's upstream retrieval
example through Promptfoo. Its dated receipts complement these captures; neither
set changes older failed attempts into successful ones. Use those latest recipes
when adopting the affected capabilities on another PC.

Active vLLM serves NVIDIA **Nemotron-3-Embed-1B-BF16**, revision
`c0c9fea93ea424587517f2c59e20db9f1d6bf615`. Actual inference returned 2,048 finite
dimensions, norm 0.9999999746521075 and eight local input tokens. HF verified
15 files; 33 extra metadata files remain an explicit warning. No active local
generation process was observed. The memory MiniLM model is a compatible baseline,
not claimed as the newest model or universal retrieval winner.

## Freshness, upgrades and savings

[Native release queries](../evidence/artifacts/full-stack-convergence-20260921/selected-upstream-releases.json)
covered all 68 selected repositories: 63 returned metadata and five returned 404.
Development pins, distro packages, prereleases and incompatible candidates do not
automatically require replacement. Current compatible upgrades retain source
commits, package integrity, relevant upstream checks, actual native results and
rollback in the wave receipt.

The selected changes are ccusage **20.0.24**, Repomix **1.18.1**, OpenResearch
**0.2.7** and Worktrunk **0.79.0**. Follow the [candidate-path installation and
rollback recipe](../recipes/native-upgrades-20260921.md). Their captured upstream
test outputs and native operation results are attached; the available selected
test subsets passed 34, 148 and seven cases respectively. Worktrunk's native
old/new comparison confirms the corrected child working directory; its Rust
suite remains unrun.

The [hosted clean-install result](../evidence/artifacts/full-stack-convergence-20260921/hosted-native-token-workflow.json)
preserves the returned GitHub runner payload from the reviewed implementation:
41 native commands, 14 checks, fresh upstream prefixes and confirmed temporary
workspace cleanup. The source hashes still match the published harness/fixtures.
This runs RTK, QMD, Repomix and TOON on local fixtures; it is distinct from the
native Claude/Codex model tasks and each vendor's complete test suite.

RTK's shared project estimate changed **2,337 → 2,371**, without exclusive provider
attribution. Context Mode text mixes retained-history and conversation scopes;
preserve raw reports and do not derive a precise combined lifetime number.
Headroom's zero native ledger is distinct from offline artifact reductions.
jCodeMunch estimates bytes avoided and can count repeated reads. TOON is chosen
per artifact; compact JSON remains smaller for the complete catalog. Read the
[counter definitions and retention](token-practice.md) before comparing values.

No finite catalog establishes universal SOTA saturation. Exact whole-PC lifetime
provider savings and improvement for every tool remain unmeasured. Convergence
means resolving supported gaps in the selected scope, retaining counterexamples,
and reopening a choice when new source or task evidence warrants it.

## Another PC or session

Start at [adoption/manifest.json](../adoption/manifest.json), select a profile,
follow upstream recipes and sign in natively. Keep source pins and rollback,
execute the useful capability and inspect its returned result, then create a
local counter ledger. Never copy authentication stores or private memory/logs.
The HTML is an offline guide; it does not launch services or install alternatives.

The [north star](../blueprints/us-equities/north-star.md) remains independently
qualified historical research/simulation before broker-specific paper operation.
Foundation results do not establish a strategy dataset, profitability, broker
deployment or production hosting.
