# Dashboard gaps: native resolution and retained evidence

This follow-up resolves the supported configuration, data-freshness and
presentation gaps identified in the [rendered review](dashboard-rendered-acceptance.md).
The original failures remain dated evidence. The installed upstream interfaces
were exercised and screenshots inspected after the changes.

| Gap | Native resolution | Evidence boundary |
| --- | --- | --- |
| Disabled memory consolidation | Existing native Codex account qualified by ai-memory plain/structured checks; both native Claude and Codex sessions automatically compiled, persisted and read back | [Memory recipe](native-memory-consolidation.md); bounded real sessions, not broad summary-quality evaluation |
| Stale selected session archive | AgentsView refreshed exactly the existing three selected sources; all 17 identities retained; messages 3,193 → 3,697 | [Archive/gateway recipe](native-dashboard-operations-resolution.md); a snapshot, not continuous transcript ingestion |
| Optional gateway unready / browser login unusable | Existing native desktop profile restored readiness and opened its authenticated dashboard through the upstream passwordless bridge | Same recipe; one ready upstream, native Models and Analytics inspected, temporary debug port closed |
| Empty current Claude panel | Real native Claude status task emitted all four provider categories; native response matched Prometheus and Grafana | [Telemetry recipe](native-telemetry-resolution.md); consumed/cached tokens, not lifetime savings |
| ntfy message formatting | Supported native template override fixes missing optional fields and timestamp line breaks; native source tests and real renderer checked | Same recipe; retained alerts unchanged; next production notification remains to be observed |
| Promptfoo only showed echo checks | Official exec provider now runs NVIDIA's published four-query retrieval example and exports actual returned scores | [Retrieval recipe](promptfoo-upstream-retrieval.md); local source-derived assertion, no MTEB or universal model-quality claim |

ai-memory's read-only wiki, global browser FTS, dated graph exports and scoped
session archives are upstream product behavior. They are documented as such.
The stronger asymmetric memory-embedding candidate remains separate from the
compatible deployed MiniLM profile; dashboard installation does not resolve
its upstream query-encoding compatibility boundary.

Use the selected upstream commands in the linked recipes. Restart only a
service whose loaded configuration changed. Keep native accounts, project
scope and unchanged evidence; another PC must collect its own results.

The authoring PC's [local resolution review](http://127.0.0.1:17500/dashboard-gaps.html)
supplements the official dashboards with raw
returned-output links and screenshots. Public GitHub evidence contains
sanitized outcomes and hashes; private transcripts and credentials stay local.
