# Comparison progress and remaining gates — September 22

The live Claude owner returned a corrected comparison packet after Codex found
broken evidence links, mixed result authorities and a publication-check defect.
All 163 sealed file hashes and 38 corrected references were checked. This is a
dated progress record. **Selected versions, candidate dispositions and canonical
layer decisions remain unchanged.** Blocked judgments are not promoted into
accepted rankings.

The [compact evidence record](../evidence/artifacts/comparison-progress-20260922/summary.json)
separates reported comparison outcomes, publication eligibility and source
authority. Retained comparison inputs and native output remain in the source
host's private seal; original hashes identify them. This public export contains
sanitized derived facts and the local gate's original and corrected source,
without raw conversations, host paths or unrelated trading collectors.

## What the returned work establishes

| Layer | Returned result | Remaining limit |
| --- | --- | --- |
| Research memory / foundation-memory | Unresolved | Reused 12-query diagnostic, missing abstention and failed byte-budget condition; not an independent holdout |
| Document retrieval | Unresolved | Shared retrieval measurements; latency and difficult-layout ingestion remain unqualified |
| Semantic retrieval | Unresolved | Retrieval numbers do not close restore acceptance; receipt assembly precedes the restore result |
| Durable memory | Overall unresolved | Two script closures say retain and pass their recorded reviewer gate, but the separate overall rule detects a failed incumbent floor |
| Runtime workers | Nine returned submission rows; unresolved | Recovery unrun, artifact acceptance unjudged, and old cancellation classification remains in the sealed run summary |
| Market data / filings | Unresolved | Identity, dated-universe, corporate-action and as-known gaps; paid challenger unrun |
| Engine parity | Reported historical run completed BLOCKED | Four failed checks attributed to dividend cash posting and market-on-open mapping; parity remains unaccepted |

The worker arms are native Claude CLI, the Codex SDK lane and Claude Agent SDK.
The scorer initially mistook a cancelled Codex submission for a usage limit;
rescored judgments preserve the earlier failed interpretation. The sealed run
summary still carries that old label. Per-submission rescored receipts are absent
from the reviewed private seal as well as this public export, so neither can
independently reconcile every rescored submission. The two judging rounds retained
refutations. No worker or SDK becomes a new accepted default from these results.

ai-memory remains the operational memory of record. Basic Memory's returned
lexical-retrieval results do not establish a replacement: both executed stores
miss the abstention floor, and the challenger misses the ownership condition.
The query transformation and exact-span proxy bound that result. Hindsight's
unrun arm has no performance verdict. A reviewer gate passing does not prove
that the comparison or a repository is generally superior.

Foundation-memory's selected measured run is timestamped 06:07:35.196 UTC.
A later degraded recording under the same protocol remains a separate attempt.
Document retrieval reuses measurements; neither reuse nor recomputation is an
independent replication. These are bounded case sets, not fresh holdouts.

## Verified correction, limited claim

The original publication check counted missing reviewer booleans as assent and
did not block a reviewer-reported identity leak. Codex and an independent reader
reproduced both failures with synthetic inputs. The corrected local script
validates reviewer booleans and blocks recorded judge/refuter leaks. The original
defective script remains preserved beside it for provenance.

The unchanged ten-test local suite requires its original source layout; running
it directly from the flat private seal fails to locate the script. The public
evidence reconstructs that layout:

```sh
node --test evidence/artifacts/comparison-progress-20260922/tests/compare/closure-script.test.mjs
```

These are locally authored synthetic contract tests, not upstream repository
acceptance. The script is retained as diagnostic evidence; it is not installed
as a new default harness. Passing these cases does not prove complete input
validation or bias-free judging. Every result retains its own authority:
mechanical closure, the durable-memory runner's composed result, the parity
harness verdict and the review publication gate answer different questions.

## Continuing toward the next workstation

The [grand handbook](grand-catalog-handbook.md) remains the setup guide.
The [research queue](../catalogs/landscape/research-state.json) retains its open
decisions. Reopen a selection with a relevant measured comparison; preserve
failed attempts, missing metrics and source provenance. Stars, newer releases
and reviewer agreement do not close these gates.

The current convergence work has not returned an explicit joint completion.
Prepare the new WSL workstation after that handoff and the user's connection,
then collect its own installation, useful-operation and recovery evidence.
macOS is a later, separately qualified target. Trading implementation and broker
acceptance remain separately owned; no account or order action follows from this
catalog progress record.
