# Runtime-reviewed catalog and clean-install handbook

## Requested outcome

Use the existing native research runtimes to independently review the grand
catalog, assess repository quality and relevant challengers, and prepare a
portable handbook with meaningful clean-install evidence for a future WSL PC.
Do not turn reviewer agreement, popularity or source review into superiority.

## Baseline and ownership

Coordinator checkout: landscape-finalization branch at
`dac2d1a3f880c04f6afadebe8bf3a251a64ab605`, continuing PR 55.
Candidate updates use a separate writing worktree; native review and clean-prefix
audits use separate read-only checkouts and private output directories.
The coordinator owns handbook, generated artifacts, integration and final checks.
Native accounts, unrelated agent-lab changes and running services are preserved.

## Completed work

- Review important runtime/memory challenger sources and fix stale exclusions;
  retain installed pins and historical evidence.
- Execute bounded native Codex and independent Claude catalog reviews using the
  existing supported runtime paths; preserve failures, source snapshots and usage.
- Recreate the locked research SDK in an isolated prefix and inspect current
  adoption prerequisites. This is same-host evidence, not new-PC acceptance.
- Integrate a handbook and quality evidence into the offline HTML; validate the
  current index, references, hashes, meaningful checks and browser behavior.
- Resolve supported independent findings before publication.

## Current evidence boundaries

The initial comparison has 188 candidate-layer records and 150 case-insensitive
repository identities. Its 20 layers comprise 8 retain, 1 adjust and 11
keep-but-compare decisions. The wider inventory has 525 identities/343 current
public stars. Counts change only through derived source records.
The exercised programmatic SDK is `openai-codex`, not OpenAI Agents SDK.
DeerFlow's existing acceptance is one embedded evidence-review call; full service
and planner operation remain unqualified. The 52-page memory restore contained
one ordinary decision and 51 System pages. Matched memory quality is unproven.

## Results and verification

Integrated candidate-source commit `d4bcae5a4a2ec34a175b07d9178d15f706430d06`
as `5a850eb`. The current union has **527 identities**, **343 current public
stars**, **207 candidate-layer rows** and **157 distinct compared repositories**.
The focused quality review covers 16 repositories and 58 immutable source files;
all 20 layer decisions remain 8 retain, 1 adjust and 11 keep-but-compare. Existing
installed pins and historical records remain dated.

The native runtime review's frozen base is the initial `dac2d1a` catalog. Codex
submitted inference but returned model capacity failure with no verdict or usage.
Claude's source-tool attempt exceeded its turn cap; a corrected tool-free attempt
returned HTTP500; one unchanged transient retry completed with Opus 5, high
requested effort, all 20 layer IDs and six source-based qualifications. Resolved
effort was unavailable. Initial Dagu branches failed; the successful final Claude
run was separate. All inputs, actual argv, failures, cache/output usage and
sanitization boundaries are retained in the runtime review artifacts. This does
not claim a native review of the later code/quality patch or cross-family agreement.

The fresh SDK prefix installed all 36 locked distributions with native uv,
required hashes, no cache and no source build. Exact inventory, seven package
imports, six SDK symbols, SQL and 55 existing data fixtures passed. The original
766-test clean-prefix suite had one missing-HOME environment error and two skips;
the targeted HOME-preserving rerun passed. The current integrated full suite
first exposed a stale 525-identity lifecycle count; it was reconciled to 527.
The final native-HOME-preserving run executed **770 tests: 768 passed, two skipped**
(separate EdgarTools environment and unavailable QEMU). These are repository
integration/fixture checks, not all upstream test suites or new-PC acceptance.

Exact final commands and scopes:

- `${SDK_ENV}/bin/python -m unittest`: 770 tests, two skips, exit 0. The suite's
  deliberate missing-quickstart negative fixture emits a failed receipt; the
  unittest process passes as expected.
- `node --test blueprints/native-skill-practice/test-contract.cjs`: six passed.
- `python3 scripts/landscape.py --root .`: 20 layers, 207 candidate rows, 157
  distinct comparisons, 16 quality reviews, 68 explained selected components.
- `python3 scripts/validate.py`, `python3 scripts/validate_catalogs.py`,
  `python3 scripts/validate_foundation.py --root . --json`: integrity and scoped
  catalog validation. No new provider/GPU qualification.
- `python3 scripts/validate_convergence.py --all-recorded --root . --json`:
  existing records plus the four-attempt native review record; declaration
  consistency is separate from execution truth.
- `python3 scripts/build_ecosystem.py --check`: exact generated-HTML equality.
- `git diff --check`: no whitespace errors.

Native agent-browser opened the generated HTML through `file://` in an owned
session, with `--allow-file-access` consistently present. Desktop 1280×900 and
mobile 390×844 showed no horizontal overflow; the page made zero resource
requests and reported no page errors. Search exercised DeerFlow and ai-memory;
the handbook opened in its embedded modal with eight sections and the corrected
memory boundary. Quality notes render for relevant candidates. New public guide
and quality-snapshot URLs use the PR branch, rather than an old commit where the
new files do not exist. This browser behavior is local UI evidence.

Independent source/code/evidence review checked snapshot joins, observed command
records, 88 baseline source hashes, native public input hashes and usage totals.
Resolved findings: duplicate candidate references; missing repo/revision/source
joins; contradictory repeated layer fields; and the stale quality-snapshot URL.
Regression mutations reject altered pins, missing snapshot repositories, changed
source URLs and contradictory layer choices. No outstanding supported findings.

Scoped Gitleaks checks returned zero findings for the new runtime-review and
quality-review artifact directories. The clean-install directory returned three
generic-key matches on source SHA256 values keyed by token-related filenames;
all three were independently recomputed from their original source files and
matched. No all-repository zero-findings scan is claimed; earlier baseline
findings retain their existing scope.

## Remaining acceptance

The handbook and recipes are ready to use when the new PC is connected. A clean
SDK prefix on this host does not qualify that PC's WSL, native sign-ins, shared
skills, scoped memory, GPU stack, Docker or complete services. Follow the ordered
profiles and retain its own results. ai-memory remains keep-but-compare against
Hindsight and Basic Memory; DeerFlow full planner/service operation remains
unqualified. No new hosting, standing schedule or broker action was performed.
