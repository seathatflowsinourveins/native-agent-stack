# Evidence-backed native skills and worker practice

## Requested outcome

Extend the grand landscape with current source-backed skill and runtime worker
choices, install the upstream TypeSafe skill using one method, and apply selected
capabilities to native Codex and Claude when the task needs them. Universal
superiority is not assumed: retain comparison limits, original sources and the
measurement that would change a decision.

## Scope and ownership

Continue PR 55 from `710ea0f2253bc4561535bc291c078c83490750fd` in its isolated
integration checkout. The coordinator owns integration, TypeSafe installation and
bounded evidence triage. Two independent read-only workers examine native runtime
worker gaps and maintained skill alternatives. Preserve unrelated local project
changes, active workers, accounts, model defaults and workflow settings.

## Acceptance

- Upstream TypeSafe installation via the requested `skills` method only, with
  agent-specific discovery and exact source identity checked.
- Each selected addition closes a stated gap; awesome lists remain discovery
  sources and recency alone is not a ranking result.
- Native worker context includes the needed skill and scoped source evidence.
  A bounded returned result verifies the selected path without promoting local
  checks into broad runtime, model-quality or trading acceptance.
- Catalog, offline HTML and new-PC recipes expose current decisions, alternatives,
  measured results, limits and remaining comparison work.
- Relevant validation and independent review pass before publication.

## Progress

TypeSafe was refreshed globally for Codex/Claude and in the project through
`npx skills add typesafe-ai/skills --skill typesafe-ai`, selecting both clients.
The project install used copies and the global Claude entry uses the installer’s
symlink. Both returned installed status at upstream revision
`65a39f393687675ce170e6094757de20370365b9`; no marketplace method was used.
The existing project practice and 16-case diagnostic are retained evidence,
including equal TypeSafe/native-Claude accuracy and unknown matched savings.

The structured supplement now selects TypeSafe plus official `gh-fix-ci` and
`security-best-practices` skills, with pinned original sources, alternatives and
activation limits. All three were installed for native Codex/Claude with the
same upstream installer and passed `skills-ref`. Their full support files and
licenses are retained locally. CI helper clean-check execution returned exit 0;
failure repair and security finding quality are not claimed.

Opt-in semantic-evidence role examples were adopted into agent-lab's native
agent directories. They inherit model/effort, select the TypeSafe context for
the task and receive no provider credential. Other pending local settings and
files were preserved. The unchanged 16-case pilot adapters/fixture were reused
for six contract checks. The first check failed because its fixture file was
not yet copied; restoring the original fixture resolved it without adapter edits.

An independent reviewer froze eight selected catalog labels before inference
at case-pack hash `7cc28c50afb9842de3d8dc923e80159bf99d0f0e9acd643684a99b61b3ad3a40`.
Eight uncached TypeSafe requests returned zero service errors and matched 7/8
labels; weak literal containment matched 3/8. Promptfoo exit 100 preserves one
TypeSafe and five baseline assertion failures. C4 remains a disagreement, with
no post-inference relabeling. The future-availability gate is illustrative and
independent of semantic support. This is a diagnostic, not a general benchmark.

Native Claude 2.1.278 completed top-level `--agent` source review with visible
Read/Glob/Grep results. It read relevant TypeSafe sections, not a proven full
preload. Initialization lists all three skills and confirms claude-fable-5-1;
effort is unavailable. Native Codex 0.155.1's first desktop-configured invocation
failed source access and returned zero case verdicts despite exit 0. The supported
Linux launcher returned a completed eight-case child review; the coordinator
report is retained, while raw child tools and resolved model/effort are unavailable.
Both completed reviews retain C4 as unqualified and C6 as rejected evidence.
Neither establishes child dispatch in both harnesses or matched savings.

The public native receipt keeps exact usage categories, failure scope and hashes
of private raw receipts. An independent read-only auditor confirmed all eight
excerpt hashes and exact local presence, distinguishing that check from hidden
Codex execution. Another reviewer found C1/C2 provider input normalization:
Promptfoo compacted JSON-valued source strings. The result now keeps frozen,
actual rendered and prior trimmed-summary hashes separately; parsed content is
equal. Original raw output, labels and inference were preserved.

The current combined index has 521 repository identities, 175 comparisons across
20 layers, 68 selected component records, 343 current public stars and 152 dated
domain cards. Three applied skills appear in the HTML with the portable recipe.
The earlier 342-star ledger and all historical wave counts retain their dates.

## Verification

- `python3 -m unittest`: 762 tests, 41 explicit skips, passed. Expected failure
  fixture output is not a new runtime failure.
- `node --test blueprints/native-skill-practice/test-contract.cjs`: six passed.
- `python3 scripts/catalog_decisions.py --write`: canonical identity/reference
  join regenerated; 521 repositories and 1,255 typed references.
- `python3 scripts/build_ecosystem.py --write`: offline HTML regenerated.
- Independent original-source/code review closed with no remaining supported
  findings after the provenance correction. The separate runtime-log audit
  independently verified all eight local excerpt hashes and qualified trace limits.
- Native browser checks passed at 1440×1000 and 390×844: the new embedded skill
  recipe opens, downloaded JSON contains all three skills and current totals,
  no page overflow, and no console/runtime errors. The owned session was closed.
- Publication integrity passed for 68 components, 1,449 hashed files, four
  profiles and 128 receipts. Catalog, foundation, landscape and identity checks
  passed, as did all 14 recorded convergence checks and the HTML rebuild check.
- Zizmor and the retained pinned actionlint passed. `git diff --check` passed.
  Gitleaks retained its prior 206 findings: 205 in unchanged files plus the known
  embedded OpenAPI checksum match in generated HTML. No clean scan is claimed.
- Publication target remains existing PR 55; no merge or new-PC acceptance is
  implied by these local checks. Hosted check results are visible on the PR.

## Remaining claim boundaries

Another PC must establish its own native sign-in, discovery and selected task
results. No universal SOTA claim, whole-pack installation, model-default change,
security audit certification or new broker acceptance follows from this work.
Optional skills activate for matching tasks. The observed TypeSafe disagreement,
failed first Codex attempt and missing native metadata remain visible.

At final local-state inspection, the adopted Claude role had subsequently gained
explicit `model: opus` and `effort: high` settings during concurrent project work.
Those edits were preserved; the portable example retains the inherited settings
used by this bounded trial. The completed receipt does not qualify that later
configuration. The adopted Codex role still matches the portable example.
