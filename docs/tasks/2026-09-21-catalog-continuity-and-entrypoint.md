# Grand catalog entrypoint, complete role coverage and continuation

## Requested outcome

Repair the nonworking HTML and finish a current, evidence-led foundation and
north-star catalog with runtime workers, selected skills, hosting/container
choices, starred/awesome discovery and continuity across PCs/sessions. Preserve
the distinction between a complete review set and comparative superiority.

## Baseline and ownership

Continue PR 55 from `66043ed0c147a397789381b0633dd899008e08c4`. One writer owned
the template/builder/tests/entry guide in a separate worktree; its commit
`76a1c901cf73d21390f9e1a8a500f063f6ec6115` was integrated as `c4960d1`.
The coordinator owned coverage, continuation and integration. Independent
read-only reviewers audited source/coverage and the substantive patch.
Existing agent-lab changes, native accounts and running services were preserved.

## Observed failure and correction

The raw `docs/ecosystem/template.html` visibly showed build placeholders and
inactive controls; its CSP/data placeholders are not a generated application.
The generated index worked. The template now shows an ordinary link to the
complete index. Controls remain hidden until startup succeeds; missing/empty
or corrupt data and a renderer failure show recovery instructions. Generated
pages never redirect automatically. Browser checks used the actual source link
and reached the complete catalog. A meaningful Node regression executes startup
logic; browser observations separately cover complete rendering.

## Coverage and selection

Six component identities lacked an explicit current comparison row despite
existing source/evidence records: MCPorter, MCP Inspector, Hugging Face Hub,
Codex for Claude, pandas and llama.cpp. They now have supporting or conditional
role explanations with actual evidence boundaries. The checker rejects any
selected component without a current comparison explanation.

Fresh original-source review added four conditional identities: Moby/Docker
Engine, Compose, Podman/Quadlet and Dockerized IB Gateway. Their exact source
commits and official guidance are retained in hosting-practice.json. The reads
occurred September 22 UTC / September 21 local. Native WSL/systemd/Dagu remain
selected; no container, paid host, new model or broker operation was installed
or qualified by this review. Pinned Nautilus documentation confirms the separate
DockerizedIBGateway start/config boundary and mutable image-tag caveat.

The resulting index has 525 repository identities and 1,272 typed references;
the landscape has 188 decisions in 20 layers, explanations for all 68 selected
components, 343 current public stars and 152 dated historical domain cards.
The 342-star canonical baseline flag remains historical. The UI already joined
the newer snapshot correctly; native browser testing confirmed Cua appears in
the current 343-star filter. Discovery breadth does not prove superiority.

## Portable continuation

The new research queue covers every layer exactly once, with an actionable next
comparison or requirement trigger, source references and bounded stopping rules.
The UI embeds both continuation and deployment guides and exposes each layer's
next action. Overall bounded closure requires each layer's closure and retained
local records; current comparative saturation remains unestablished. Structural
validation does not prove a closure record's substantive quality.

Adoption references point to the current queue and skill/deployment records.
Existing foundation gate-ID semantics remain intact: its empty required-gate
list does not mean comparative saturation. Research references are separate.
The historical LEAN/SDK profile is labeled explicitly; the Nautilus profile and
runtime-target govern the selected north star. The old September 19 landscape
page is marked historical with current forward links.

## Verification and retained limitations

- Entrypoint worker: 40 ecosystem checks passed, with raw/missing/empty/corrupt
  data and renderer-failure startup cases. Real browser source-link, recovery and
  mobile checks passed. An initial navigation race was resolved by waiting for
  the visible application, not by changing product behavior.
- Integrated full suite: 765 tests passed with 41 environment-dependent skips
  before the final status-text regression was added. All 60 affected ecosystem,
  landscape and adoption checks passed afterward. The final full suite passed
  766 tests with the same 41 explicit skips.
- Independent review found and resolved a closed-state UI message that still
  said research was open, and a Hugging Face acquisition claim lacking its direct
  download receipt. Existing pinned evidence was added; no inference was rerun.
- Integrated browser checks verified 525 repositories, 188 decisions, both
  embedded guides, per-layer continuation, and downloaded JSON exactly matching
  embedded data with 20 research records and 68 component joins. Current-star
  filtering returned 343 and included Cua. Desktop 1280 and mobile 390 had no
  document overflow, page errors, console messages or background requests.
  An unsupported automation selector was replaced by the current snapshot ref;
  this was not a product failure. The owned browser session was closed.
- Final integrity/generated-page/CI results are checked against the published
  revision. Existing native and provider receipts retain their dates and limits.
- Final local integrity passed for 1,454 hashed files, 68 components, four
  historical profiles and 128 receipts. Catalog, foundation, landscape, identity,
  all 14 declared convergence records and exact HTML rebuild checks passed.
  Whitespace checks passed. Gitleaks retained 206 baseline findings: 205 in
  unchanged files and the known generated OpenAPI checksum match; no clean scan
  is claimed. Independent review closed with no remaining actionable findings.

No new physical PC, hostile-container boundary, production hosting, comparative
runtime/model win or strategy profitability was established. Future work selects
one unresolved requirement from the queue and collects that task/host's evidence.
The package creates no unattended research schedule or automatic installation.
