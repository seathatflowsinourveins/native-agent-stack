# Template for ${XDG_CONFIG_HOME:-$HOME/.config}/native-agent-stack/alpaca-paper.env
# Create with: install -m 600 docs/examples/alpaca-paper.env.example "<store>/alpaca-paper.env"
# then fill the values in a local editor. Paper keys only; never live keys.
export APCA_API_KEY_ID=
export APCA_API_SECRET_KEY=
# Optional, not secret:
# export APCA_API_BASE_URL=https://paper-api.alpaca.markets
