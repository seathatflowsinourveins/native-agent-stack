# Independent findings by layer

Both source reports were sealed before coordinator comparison. The table records
the current synthesis and retains explicit unresolved comparisons. Claude's
final coverage return and later screening repair are complete. The [final adjudication](claude-blind-adjudication-20260921.md) records audit corrections and remaining comparison gates. No row claims a
controlled runtime-quality win, zero bias or destination-PC acceptance.

The current disposition mirrors `layers[].decision` in the canonical [foundation](../catalogs/landscape/foundation.json) and [north-star](../catalogs/landscape/us-equities.json) catalogs. The [September 22 reconciliation](catalog-reconciliation-20260922.md) corrected nine ambiguous summary labels; historical peer wording is preserved as evidence. Candidate dispositions and each host's receipts govern adoption.

| Layer | Current disposition | Codex source adjudication | Claude source adjudication |
| --- | --- | --- | --- |
| native-clients | retain | Native Codex/Claude match both the installed evidence and the independent interface-fit recommendation. Aider is newly indexed as a provider-flexible alternative; no equivalent account/recovery trial establishes a replacement. | Retain Codex/Claude. Claude Code was wrongly excluded by the mechanical license check despite explicit commercial terms. Star-capped rankings and mixed benchmark/model results do not establish a better native-client replacement. |
| instructions-skills | adjust | The review favors the Agent Skills format and Anthropic examples; these complement the selected small skill set. Superpowers remains a workflow candidate, not a reason to load another full instruction suite. | Retain targeted shared skills. Broad agent-framework discovery did not run a controlled skill-use comparison; omitted smaller skill repositories were not tested failures. |
| workers | keep but compare | Both views favor native Codex/Claude for bounded work. Keep application SDKs and LangGraph conditional on a real application/durable-state requirement. Claude's portable roles/workflow evidence is now integrated. | Retain native owned workers. DeerFlow, Deep Agents and OpenHands SDK were omitted from F-agents, while application SDKs lost the stars cutoff. Preserve these conditional comparisons and distinguish Agent Canvas hosting from its SDK and historical benchmark harness. |
| isolation | retain | Git ownership, client restrictions and container isolation are different responsibilities. Worktrunk remains the accepted ownership helper; native sandbox restrictions require their own enforcement tests. Rootless Podman is conditional. | Retain worktree ownership plus qualified client restrictions. Container Use and OpenShell add discovery leads; early-development and experimental WSL support require actual isolation, cancellation and cleanup checks. |
| code-navigation | retain | The independent exact/structural/symbol stack agrees with the current responsibility split. ripgrep and SCIP are newly indexed; SCIP needs language/index maintenance evidence before adding another lane. | Retain exact/symbol/structural lanes. Serena was discovered but excluded from the star-capped shortlist; the reviewer explicitly identified inadequate coverage of small code-navigation tools. |
| document-retrieval | keep but compare | QMD agrees. Docling leads the review only when layout/tables require it; the current lightweight accepted conversion remains appropriate until a representative document set demonstrates a useful extraction advantage. | Keep QMD and accepted lightweight conversion; compare Docling when layout matters. Its governance, technical report and models support source credibility, not superior extraction on our documents. |
| semantic-rag | keep but compare | The review proposes code-memory for smaller local corpora, explicitly at low confidence. Independent implementation inspection confirms a simpler SQLite deployment case, but the incumbent already has hybrid fusion. New-ignore/all-files-deleted cleanup risks and separate default-model licensing require qualification. Add a conditional comparison; no runtime or ranking result supports replacing the accepted route. | Keep SocratiCode/Qdrant. Qdrant is not uniquely minimal: Milvus Lite and LanceDB are relevant deployment comparisons. No same-corpus quality, maintenance or resource test displaces the current scoped index. |
| durable-memory | keep but compare | Basic Memory's inspectable Markdown and local cross-client interface make it a priority challenger. The independent review did not inspect ai-memory or Hindsight, so it cannot disqualify either. Retain the accepted ai-memory store pending the already required matched recall/deletion/restore comparison. | Keep ai-memory pending matched comparison. WF2 omitted ai-memory and Basic Memory; Hindsight was discovered. Mem0 managed-platform benchmarks do not qualify its open-source SDK, and a LoCoMo study of other systems does not rank our store. |
| web-research | retain | Playwright and Tavily's SDK complement the accepted CLI/browser tools; API libraries do not inherit native tool or account qualification. Crawl4AI stays conditional on a crawling workload. This report did not adjudicate DeerFlow or full research runtimes. | Retain native browser/search tools. Browsertrix adds an archival-evidence comparison; Playwright traces support debugging rather than deterministic retrieval replay. Browser-agent scores use different models and protocols. |
| token-efficiency | keep but compare | Both approaches favor scoped exact retrieval and explicit-file packaging. Context Mode, QMD and other accepted helpers remain selected by task. No report measured causal provider savings. | Retain scoped retrieval and guarded transformations. LLMLingua is a source-pinned challenger; no measured preservation of our exact facts or complete provider cost supports substitution. |
| quality-evaluation | retain | Add Hypothesis and DeepEval to discovery alongside pytest/Playwright/Promptfoo. They address generated edge cases and model evaluations, not proof that an untested repository wins; project/upstream acceptance remains the gate. | Retain project/upstream checks and selected evaluation tools. Inspect AI remains a scoped challenger. A repository's test count, badges or reviewer score is not demonstrated result quality. |
| ci-supply-chain | retain | zizmor, actionlint and Syft agree. Cosign and Trivy are complementary candidates; existing attestations, secret checks and actual workflow evidence retain their precise scopes. | Retain accepted analyzers, inventory and attestations. Grype/cosign/Trivy remain task-specific sources. Scorecard properties and CI presence cannot replace the actual relevant check or prove broad security. |
| scheduling-supervision | keep but compare | systemd agrees for local service ownership. Dagu has retained local workflow/recovery evidence that the independent report did not examine. Prefect/Temporal need a durable pipeline or cross-host recovery requirement plus matched lifecycle acceptance. | Keep Dagu/systemd for the qualified local responsibilities. Workflow-framework rankings did not test our effect ownership, retry, cancellation or recovery; use Temporal/Restate/Prefect only for a demonstrated gap. |
| hosting-services | retain | FastAPI agrees for Python interfaces; Pydantic supplies typed contracts. Caddy and Hono are newly indexed optional service/application choices. They do not replace a qualified UI/database stack or justify public hosting. | Retain the qualified application/service choices. The broad F-ops group gives limited direct application-stack comparison; container/gateway popularity does not justify replacing Next.js/React/FastAPI/PostgreSQL. |
| recovery-portability | keep but compare | uv, mise and Restic align. Nix is newly indexed for a demonstrated system-level reproducibility gap; reproducible artifacts and this host's restore do not certify a fresh WSL machine. | Keep uv/native recipes and Restic. Refuter discovery independently identifies Restic, but source agreement is not a restore on a new WSL host. Retain destination installation and recovery checks. |
| observation-inference | keep but compare | OTel and otel-tui align; Collector core and Contrib have different scope. llama.cpp and CPU-capable vLLM are workload alternatives, not mandatory infrastructure. Retain the accepted inference pin until target-hardware/model acceptance. | Retain telemetry and the compatible inference pin. Prometheus is operations telemetry, not authoritative order/accounting state. New vLLM determinism controls have hardware and performance limits requiring separate qualification. |
| foundation-memory | keep but compare | The source-first filings/documents/code proposal complements the memory/retrieval division. EdgarTools and DuckDB belong to provenance/analysis; they do not substitute for cross-session knowledge ownership. | Retain complementary memory and source-retrieval responsibilities. Versioned tables and temporal graphs do not establish when vendor information was actually knowable; representative financial retrieval and supersession checks remain open. |
| agents-operations | retain | Native clients and systemd agree. Prefect and LangGraph are conditional research-application choices; the accepted Dagu/telemetry/recovery path has execution evidence the independent source report did not inspect. | Retain native research/review, local effect ownership and shared telemetry. No comparative runtime trial in WF2 establishes migration to a framework, gateway or hosted stack. |
| data-research | keep but compare | Databento's coverage/reference-data case is a real challenge to choosing a provider solely for broker convenience. Keep the qualified bounded Alpaca path, and compare rights, point-in-time universe, corporate actions, corrections and total cost on identical securities/dates before changing provider. | Keep bounded official Alpaca/EdgarTools/local analysis and compare Databento for requirements. LEAN samples are not a full point-in-time universe; storage versioning alone does not establish publication-time correctness. |
| engines-strategies | keep but compare | LEAN has inspectable engine, equity tests and both broker plugins, which strengthens its comparison case. Keep the user's Nautilus destination with explicit broker/accounting gaps. Source-test weaknesses must be checked narrowly; neither an engine feature list nor a model preference establishes paper readiness or strategy performance. | Keep Nautilus destination and LEAN comparison. LEAN equity actions/borrow remain a strong challenge. Issue #4983 reports the Rust IB path at v1.227.0 and explicitly distinguishes Python; no whole-engine or selected-version failure is inferred. Alpaca remains a separate qualified path. |

See [source corrections and evidence](claude-blind-adjudication-20260921.md) and
the [portable handbook](grand-catalog-handbook.md). Reopen a choice using its
canonical layer comparison, not popularity, omission or reviewer agreement.
