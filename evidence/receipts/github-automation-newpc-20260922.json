{
  "schema_version": 1,
  "id": "github-automation-newpc-20260922",
  "kind": "local_integration",
  "recorded_at_utc": "2026-09-22T00:00:00Z",
  "component_ids": [],
  "claim": "The new-machine handbook (adoption/bootstrap.md, adoption/platforms/*.md), the three native client config templates and tools/adoption/render_config.py were built and locally verified on this Linux/WSL2 host: render_config.py --check reproduced this host's live ~/.claude/settings.json, ~/.codex/config.toml and agent-lab/.codex/config.toml byte-for-byte from the templates plus this host's own gitignored adoption/hosts/wsl-seath.json values, and the new unit tests plus every existing repository validator pass against the three new catalogs/foundation/decisions.json rows. Hosted GitHub Actions execution for the new-pc-bootstrap-ci decision is explicitly pending, not claimed here.",
  "limitations": [
    "Byte-identical --check is native evidence for this one host's three live files only; a different host still needs its own adoption/hosts/<host>.json and its own --check run.",
    "adoption/platforms/macos-arm64.md is drafted, not accepted: no command on that page has run on macOS.",
    "new-pc-bootstrap-ci is source_review: no hosted GitHub Actions workflow or run exists yet for this bootstrap sequence.",
    "This receipt does not claim any provider/model task, account readiness check, or service start; it is structural/local-integration evidence only."
  ],
  "data": {
    "render_check": {
      "command": "python3 tools/adoption/render_config.py --host wsl-seath --check --live-codex-project <agent-lab checkout>/.codex/config.toml",
      "exit_code": 0,
      "result": "settings.json, codex.config.toml and project.codex.config.toml all reported byte-identical to their live files",
      "host_value_file": "adoption/hosts/wsl-seath.json (gitignored; not published; matches adoption/hosts/example.json's key set with this host's real values)"
    },
    "diff_outcome": "byte_identical",
    "diff_outcome_note": "No JSON-formatting-only fallback was needed; the rendered and live bytes matched exactly for all three files.",
    "commands_run": [
      {"command": "rtk proxy python3 scripts/validate.py", "scope": "structural"},
      {"command": "rtk proxy python3 scripts/validate_catalogs.py", "scope": "structural"},
      {"command": "rtk proxy python3 scripts/validate_foundation.py --root . --json", "scope": "structural, covers the three new decision rows"},
      {"command": "rtk proxy python3 scripts/landscape.py --root .", "scope": "structural"},
      {"command": "rtk proxy python3 scripts/validate_convergence.py --all-recorded --root . --json", "scope": "structural, unchanged records"},
      {"command": "rtk proxy python3 -m unittest tests.test_render_config tests.test_adoption_contract -v", "scope": "new/extended local integration tests"},
      {"command": "rtk proxy python3 -m unittest", "scope": "full repository suite"}
    ],
    "hosted_ci_status": "pending: no GitHub Actions workflow exists yet for new-pc-bootstrap-ci; this row is review_status source_review in catalogs/foundation/decisions.json",
    "macos_status": "pending: adoption/platforms/macos-arm64.md is drafted_not_accepted; macos-embedding-backend is review_status source_review"
  },
  "host_scope": {
    "profile": "source-host-linux-wsl",
    "platform": "Linux/WSL2 x86_64",
    "identity": "Private source-host identity omitted",
    "import_limit": "Recorded source-host Linux/WSL2 execution only; importing this receipt does not qualify installation or behavior on macOS or a second physical host."
  }
}
