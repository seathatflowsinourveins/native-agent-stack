# Local coding tools (Linux / WSL)

Carry out the user's authorized task directly. For bounded fixes, setup, checks and documentation changes, use a short internal plan and continue through the useful result and relevant verification. Do not introduce intake, brainstorming approval, separate planning approval, a diagnostic campaign, or a restart unless the task actually requires it. Reuse installed upstream commands and passing evidence whose inputs and scope still match; load only what the current action needs. Optional catalog entries do not require activation in every task.

Use the installed upstream tools when they fit: `ast-grep` for structural code patterns; `qmd search` then `qmd get` for indexed local documents; `repomix` with explicit file selection for a code handoff. Keep ordinary `rg`/Serena for focused discovery. Select one useful lane per artifact.

The `agent-lab-docs` QMD index/collection covers `<PROJECT>/docs`: use `qmd --index agent-lab-docs ... -c agent-lab-docs`; refresh with `qmd --index agent-lab-docs update`. Its current setup uses local BM25 without downloaded models. Choose another explicit collection for other projects.

Repomix `--compress` omits implementation details; read original code before editing or deciding correctness. Context Mode/RTK counters estimate savings; provider usage is separate. Read `<ECOSYSTEM_HOME>/docs/native-additions.md` only when setup, commands or scope are needed.

Use `markitdown` for selected local HTML-to-Markdown conversion (base install; PDF/Office extras absent). For static call graphs when Serena lacks a language, use codebase-memory via native `mcporter`; for retained Context Mode CLI calls, use the same upstream bridge. `<ECOSYSTEM_HOME>/config/mcporter.json` is scoped to agent-lab, not other repositories; see `<ECOSYSTEM_HOME>/docs/native-session-commands.md` for native commands, index refresh and per-project adoption.

Use native `agent-browser` for selected browser operations and scoped `agentsview` for historical Codex/Claude session retrieval. Read `<ECOSYSTEM_HOME>/docs/native-ecosystem-activation.md` on demand for their configuration and actual results, Bun/Context Mode support, and Claude HUD status. Session archives require explicit scope and refresh; keep generated service keys private.

Use `difft` for selected structural comparisons and `shellcheck` for shell maintenance. Read `<ECOSYSTEM_HOME>/docs/native-client-e2e.md` on demand for native client results, exact commands and the optional Codex-for-Claude review bridge. Check native account readiness before delegation; prior quota errors are dated evidence.

Use installed `bd` for a selected dependency-aware task queue, `otel-tui` for local telemetry inspection, and `skills-ref` for portable skill-format reference checks. Exact upstream commands and limits are in `<ECOSYSTEM_HOME>/docs/native-upstream-additions.md`; load it only for those tasks. Keep each Beads database explicitly scoped and each telemetry listener on loopback. The reference validator does not define Claude/Codex extension support.

Use native `zizmor --offline --no-config --no-ignores --no-progress --persona regular --strict-collection --format json .github/workflows` for selected GitHub workflow checks; it reports findings without running workflows.


For native token-saving practice and cumulative evidence in Linux/WSL, read `<ECOSYSTEM_HOME>/docs/token-practice.md` on demand. After meaningful efficiency changes or when reporting savings, run `ecosystem-token-report refresh`; it captures upstream counters and updates the full HTML/catalog view. Keep native estimates, unique artifact comparisons and provider trials separate. TOON has no lifetime counter; retain compact JSON when TOON expands it. Do not preload the full catalog into each session.


For scoped jCodeMunch code retrieval, guarded Headroom and the supported Context Mode project-root repair, read `<ECOSYSTEM_HOME>/docs/native-token-workflow.md` on demand. The canonical portable grand catalog and practice live at https://github.com/seathatflowsinourveins/native-agent-stack. Use its recipes for another PC and collect that PC's own evidence.

@RTK.md

For future native sessions, the persistent Linux/WSL tool defaults are in the client settings and shell profile. Use the installed tools for the task without copying a startup prompt. Read `<ECOSYSTEM_HOME>/docs/token-session-setup.md` only for setup or accounting. Keep MCP indexes and memory scoped to the selected project; another repository needs its own adoption configuration.

<!-- native-harness-defaults:start -->
Use native Codex/Claude and supported upstream commands as the default harness. Select capabilities from the task and acceptance evidence; preserve native accounts, models, permissions and scoped state. Use bounded independent workers with owned writing checkouts. Reuse valid receipts, retain failures and source recovery, and verify the changed capability before adoption. Read `<STACK_CHECKOUT>/docs/harness-defaults.md` for harness decisions and the two catalog boundaries. Load only the relevant layer; a dated catalog or newer release is not universal superiority. Continue authorized work without repeated setup or approval loops.
Research and reuse maintained upstream skills, examples, tests and automation before custom orchestration. Preserve source pins and actual native command results; label local integration checks and synthetic fixtures separately. Follow the linked acceptance evidence policy; local tests or generated receipts alone do not establish upstream E2E.
<!-- native-harness-defaults:end -->
