{
  "id": "sota-refresh-20260923-pins-mem-markitdown",
  "component": "markitdown",
  "repository": "https://github.com/microsoft/markitdown",
  "from_pin": "0.1.7",
  "to_version": "0.1.8",
  "release_published_at": "2026-09-21T21:22:51Z",
  "preregistration": {
    "written_at": "2026-09-23T00:26:30Z",
    "note": "Written concurrently with running the checks below, after resolving the target version via `gh api repos/microsoft/markitdown/releases/latest`.",
    "expectation": "0.1.8 installs into an isolated uv venv, reports `markitdown 0.1.8` on `--version`, and converts a Markdown-adjacent source file to Markdown text of a plausible size, matching the two functional checks the retained receipt recorded for 0.1.7 (evidence/artifacts/claude-repository-evidence-20260921/results.json, id=markitdown): version-help and a document-conversion functional check with assertion int >= 100 bytes.",
    "criteria": "pass/qualified: `markitdown --version` contains \"0.1.8\"; converting a text file to Markdown exits 0 and produces >=100 bytes of output. fail: version mismatch, non-zero exit, or output below the retained threshold."
  },
  "acceptance_check_ref": "evidence/artifacts/claude-repository-evidence-20260921/results.json (component id=markitdown, commands: `markitdown --version` asserted contains \"0.1.7\"; `markitdown README.md > ... && wc -c` asserted int >= 100, observed 2191)",
  "isolation": {
    "prefix": "$HOME/.cache/sota-refresh-20260923/pins-mem/venvs/markitdown-0.1.8",
    "install": "uv venv + uv pip install markitdown==0.1.8 into a unit-owned venv, never the shared tools/markitdown-0.1.7 uv-tool prefix the current pin uses"
  },
  "commands": [
    "gh api repos/microsoft/markitdown/releases/latest",
    "uv venv $HOME/.cache/sota-refresh-20260923/pins-mem/venvs/markitdown-0.1.8 --python 3.13",
    "ecosystem-bounded-run uv pip install --python <venv>/bin/python markitdown==0.1.8",
    "<venv>/bin/markitdown --version",
    "<venv>/bin/markitdown README.md > $TMP/readme.md && wc -c < $TMP/readme.md"
  ],
  "results": {
    "latest_release": "tag_name=v0.1.8, published_at=2026-09-21T21:22:51Z.",
    "install": "uv pip install succeeded (markitdown==0.1.8 plus dependencies: magika 0.6.3, markdownify 1.2.3, onnxruntime 1.30.0, etc.).",
    "version_check": "`markitdown --version` -> `markitdown 0.1.8`. Assertion 'contains \"0.1.8\"' passed.",
    "conversion_check": "`markitdown README.md` (this worktree's README, a different source file than the original run's README but the same class of fixture: a Markdown/HTML-ish local document) produced 21363 bytes of output, exit 0. Assertion 'int gte 100' passed (retained receipt observed 2191 bytes on a different README; both satisfy the same >=100-byte threshold)."
  },
  "verdict": "qualified",
  "evidence_class": "native_proven",
  "limits": [
    "The conversion fixture is this worktree's README.md, not byte-identical to the file converted in the retained 0.1.7 receipt (a different repository checkout's README at that time); the assertion class (int gte 100) and command shape are unchanged, but this is not an exact-fixture re-run.",
    "Only the two commands the retained receipt recorded (version, one HTML/Markdown conversion) were re-run; markitdown's broader PDF/Office/OCR surface remains out of scope per the retained receipt's own noted boundary.",
    "No network document sources or credentials were used; local-only conversion."
  ],
  "checked_at": "2026-09-23T00:27:00Z"
}
