Read-only review complete (Read tool only; no edits, commands, delegation, browsing, or account files). Both files read in full.

## Findings (5)

**1. `foundation-closure-20260921.md` → "Final candidates by foundation layer": unpinned links, and at least two owner paths that look wrong.**
The source-convergence table pins every entry to a commit SHA; the layer table links 30+ bare `github.com/<owner>/<repo>` URLs with no pin, no as-of date, and no observation method — while the same document's policy is "Retain source pins" and "Exact versions … remain in the linked manifests." Two are concretely suspect against the owners normally associated with those projects: Beads as `gastownhall/beads` (line 51) and Dagu as `dagucloud/dagu` (line 61). Several others I cannot place at all (`agentskills/agentskills`, `giancarloerra/SocratiCode`, `andrewyng/context-hub`, `chopratejas/headroom`, `rtk-ai/rtk`, `kenn-io/agentsview`). Same gap in "SDK and runtime decisions": "Candidate checked now", "Installed 2.1.278 / 0.155.1 match checked release metadata", and registry versions 0.2.157 / 0.3.278 carry no retrieval date or command, so currentness cannot be re-verified or expired — only the Python registry/release divergence (line 74) is disclosed. Fix: pin or datestamp each layer-table link the way the convergence table does, and record how the version readings were obtained.

**2. `foundation-closure-20260921.md` → "Actual session and terminal evidence": a zero `/usage` reading cannot close the usage-observation boundary.**
Lines 106–109 report "zero API duration and zero input/output/cache tokens **before any model prompt**" and conclude this "closes the earlier unobserved interactive context, usage and HUD boundary on this host." Zeros before any prompt show the panel renders a default; they do not exercise the accounting path. The context and HUD parts of that claim are supported by the enumerated `/context all` figures; the usage part is not. Fix: either narrow the sentence to context + HUD rendering, or record one non-zero `/usage` reading after a known prompt.

**3. `restricted-worker-compatibility.md` → paragraph 2: a two-commit "matches" claim backed by one hash.**
Lines 10–13 assert installed `routing.mjs` matches both tagged commit `589d8214…` and inspected current-main `3053ca52…`, but the only recorded artifact is the installed file's SHA-256 (`7447ee2e…`). Neither upstream blob hash nor the comparison method is stated, and the relationship between Context Mode "1.0.169" and those commits is not recorded. As written the reader must take the equality on assertion. Fix: record the two upstream blob hashes (or the diff result) and the tag↔commit mapping.

**4. `restricted-worker-compatibility.md` → "Accepted inherited-tool recovery and post-hook permissions": single-trial qualification with an unresolved nondeterminism still open.**
Lines 39–45 say the inherited-tool trial "qualifies the recorded role, task and same-host supervisor boundary" from one receipt, with no run count or repetition. The preceding section (lines 25–27) explicitly leaves unexplained *why* the child prepended `ls -la &&` given an identical 4,818-character prompt — i.e. an acknowledged nondeterministic child-behavior failure mode. The acceptance paragraph never states whether that mode was re-tested under the inherited-tool role. Two residuals in the same paragraph — "one final local effect" and "no direct descendant PID cleanup" — are recorded without saying whether they are accepted-by-design or open defects. Fix: state trial count, whether the deviation recurred, and the disposition of those two residuals.

**5. `restricted-worker-compatibility.md` → lines 3–6 and 31–35: restricted roles are left with no named enforcement control.**
The document's default is correct in direction (inherit client tools; a tool list is not an OS sandbox) and correctly forbids patching the plugin or loosening global permissions. But its only instruction for a deliberately restricted role is "qualify any deliberately restricted role against its actual loaded hooks" — it names no control that actually constrains one. Directory ownership, worktrees and process supervision are cited as the real controls (line 5) but never mapped to a read-only or write-restricted role (e.g. read-only checkout/mount, separate OS user, no-write worktree). Given issue #946 is open and no supported opt-out exists, the practical default for such roles is an unenforced prompt contract — the exact condition this review itself is operating under. Fix: add the concrete control per restricted role class, or state plainly that restricted roles are unsupported until #946 resolves.

## Limits

- No network, no command execution, no repository inspection: every upstream URL, commit SHA, blob hash, version number, issue number (#946, #832) and linked artifact (`containment-receipt.json`, `native-exact-read-receipt.json`, `native-rewrite-diagnosis.json`, manifests, acceptance plan) is **unverified**. Finding 1's suspect owners are a flag for verification, not a confirmed error; findings 3 and 4 are about what the documents record, not about whether the underlying trials passed.
- I read only the two named files; cross-document claims were checked against each other only. The closure doc's recovery/cancellation boundaries (lines 51, 86–88) are consistent with the compatibility doc's (lines 44–45) — no conflict found there.
- Savings language is handled well and I found nothing to fault: no causal lifetime or whole-session saving is claimed, counters are kept separate, and "more reasoning" is explicitly not framed as a saving. The UI context figures are correctly labeled rounded estimates, not a provider bill.
- North-star section (lines 143–154) correctly keeps replay, IBKR paper and Alpaca paper separate and does not treat catalog inclusion as live configuration; no finding.
- Existing CLI recovery evidence does not establish any SDK or role beyond the one recorded — the closure doc states this itself (lines 86–88) and I did not find that boundary overstepped.

Continuity: review-contract = foundation-20260921; provider savings unknown; paper brokers remain independently qualified.
