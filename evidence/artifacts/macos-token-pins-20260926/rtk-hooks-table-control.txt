# rtk exclusion instruction: a second [hooks] table against the value set inside the one table, 2026-09-26T12:19:49Z
# Python 3.13.15; scratch directory shown as <scratch>; no network, nothing installed
# rtk binary copied to <scratch>/bin/rtk: sha256 23433a2a50bdeb12199cadd4b94b639238d6c38529fcba1c30c9295db5fa9517; `rtk --version`: rtk 0.50.0 (exit 0)
# expected sha256 23433a2a50bdeb12199cadd4b94b639238d6c38529fcba1c30c9295db5fa9517, the "extracted rtk sha256" line of rtk-config-path.txt
# recipes/README.md block (sha256 1f6126b4fd0e63d089f2869440d2c80533e6af5b121d655353c5af182cfcccef), 4 entries:
[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
# environment of every rtk run: env -i HOME=<scratch>/scratch-home PATH=/usr/bin:/bin RTK_TELEMETRY_DISABLED=1

## the 2026-09-25 two-entry key, old instruction
### starting file
[hooks]
exclude_commands = ["^git show [^ ]*:", "diff"]
### <scratch>/scratch-home/.config/rtk/config.toml after the old instruction
[hooks]
[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
$ rtk config
Config: <scratch>/scratch-home/.config/rtk/config.toml

rtk: TOML parse error at line 2, column 1
  |
2 | [hooks]
  | ^
invalid table header
duplicate key `hooks` in document root
[exit 1]
[check: the 2026-09-25 two-entry key, old: rtk config exits 1 with 'duplicate key `hooks` in document root': ok]
$ rtk hook check 'git show HEAD:x | tail -n 5'
rtk git show HEAD:x | tail -n 5
[exit 0]
[check: the 2026-09-25 two-entry key, old: the hook rewrites the probe with its defaults: ok]

## the 2026-09-25 two-entry key, new instruction
### starting file
[hooks]
exclude_commands = ["^git show [^ ]*:", "diff"]
### <scratch>/scratch-home/.config/rtk/config.toml after the new instruction
[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
$ rtk config
Config: <scratch>/scratch-home/.config/rtk/config.toml

[tracking]
enabled = true
history_days = 90

[display]
colors = true
emoji = true
max_width = 120

[filters]
ignore_dirs = [
    ".git",
    "node_modules",
    "target",
    "__pycache__",
    ".venv",
    "vendor",
]
ignore_files = [
    "*.lock",
    "*.min.js",
    "*.min.css",
]

[retriever]
mode = "sqlite"
max_entry_bytes = 10485760
max_entries = 200
retention_days = 30
compression = true
tee_max_files = 20
tee_max_file_size = 1048576
tee_on_success = false

[telemetry]
enabled = false

[hooks]
exclude_commands = [
    "^git show [^ ]*:",
    "diff",
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
transparent_prefixes = []
suppress_hook_warning = false

[limits]
grep_max_results = 200
grep_max_per_file = 25
status_max_files = 15
status_max_untracked = 10
passthrough_max_chars = 2000

[awareness]
level = "default"
[exit 0]
[check: the 2026-09-25 two-entry key, new: rtk config exits 0 and reads the file: ok]
[check: the 2026-09-25 two-entry key, new: the effective config holds the four entries: ok]
$ rtk hook check 'git show HEAD:x | tail -n 5'
No rewrite for: git show HEAD:x | tail -n 5
[exit 1]
[check: the 2026-09-25 two-entry key, new: the hook leaves the probe alone: ok]

## a multi-line key beside another [hooks] key, after a [tracking] table, old instruction
### starting file
[tracking]
enabled = true
history_days = 30

[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
]
suppress_hook_warning = true
### <scratch>/scratch-home/.config/rtk/config.toml after the old instruction
[tracking]
enabled = true
history_days = 30

[hooks]
[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
suppress_hook_warning = true
$ rtk config
Config: <scratch>/scratch-home/.config/rtk/config.toml

rtk: TOML parse error at line 6, column 1
  |
6 | [hooks]
  | ^
invalid table header
duplicate key `hooks` in document root
[exit 1]
[check: a multi-line key beside another [hooks] key, after a [tracking] table, old: rtk config exits 1 with 'duplicate key `hooks` in document root': ok]
$ rtk hook check 'git show HEAD:x | tail -n 5'
rtk git show HEAD:x | tail -n 5
[exit 0]
[check: a multi-line key beside another [hooks] key, after a [tracking] table, old: the hook rewrites the probe with its defaults: ok]

## a multi-line key beside another [hooks] key, after a [tracking] table, new instruction
### starting file
[tracking]
enabled = true
history_days = 30

[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
]
suppress_hook_warning = true
### <scratch>/scratch-home/.config/rtk/config.toml after the new instruction
[tracking]
enabled = true
history_days = 30

[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
suppress_hook_warning = true
$ rtk config
Config: <scratch>/scratch-home/.config/rtk/config.toml

[tracking]
enabled = true
history_days = 30

[display]
colors = true
emoji = true
max_width = 120

[filters]
ignore_dirs = [
    ".git",
    "node_modules",
    "target",
    "__pycache__",
    ".venv",
    "vendor",
]
ignore_files = [
    "*.lock",
    "*.min.js",
    "*.min.css",
]

[retriever]
mode = "sqlite"
max_entry_bytes = 10485760
max_entries = 200
retention_days = 30
compression = true
tee_max_files = 20
tee_max_file_size = 1048576
tee_on_success = false

[telemetry]
enabled = false

[hooks]
exclude_commands = [
    "^git show [^ ]*:",
    "diff",
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
transparent_prefixes = []
suppress_hook_warning = true

[limits]
grep_max_results = 200
grep_max_per_file = 25
status_max_files = 15
status_max_untracked = 10
passthrough_max_chars = 2000

[awareness]
level = "default"
[exit 0]
[check: a multi-line key beside another [hooks] key, after a [tracking] table, new: rtk config exits 0 and reads the file: ok]
[check: a multi-line key beside another [hooks] key, after a [tracking] table, new: the effective config holds the four entries: ok]
[check: a multi-line key beside another [hooks] key, after a [tracking] table, new: the effective config keeps 'history_days = 30': ok]
[check: a multi-line key beside another [hooks] key, after a [tracking] table, new: the effective config keeps 'suppress_hook_warning = true': ok]
$ rtk hook check 'git show HEAD:x | tail -n 5'
No rewrite for: git show HEAD:x | tail -n 5
[exit 1]
[check: a multi-line key beside another [hooks] key, after a [tracking] table, new: the hook leaves the probe alone: ok]

## a [hooks] table without the key, old instruction
### starting file
[hooks]
suppress_hook_warning = true
### <scratch>/scratch-home/.config/rtk/config.toml after the old instruction
[hooks]
suppress_hook_warning = true

[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
$ rtk config
Config: <scratch>/scratch-home/.config/rtk/config.toml

rtk: TOML parse error at line 4, column 1
  |
4 | [hooks]
  | ^
invalid table header
duplicate key `hooks` in document root
[exit 1]
[check: a [hooks] table without the key, old: rtk config exits 1 with 'duplicate key `hooks` in document root': ok]
$ rtk hook check 'git show HEAD:x | tail -n 5'
rtk git show HEAD:x | tail -n 5
[exit 0]
[check: a [hooks] table without the key, old: the hook rewrites the probe with its defaults: ok]

## a [hooks] table without the key, new instruction
### starting file
[hooks]
suppress_hook_warning = true
### <scratch>/scratch-home/.config/rtk/config.toml after the new instruction
[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
suppress_hook_warning = true
$ rtk config
Config: <scratch>/scratch-home/.config/rtk/config.toml

[tracking]
enabled = true
history_days = 90

[display]
colors = true
emoji = true
max_width = 120

[filters]
ignore_dirs = [
    ".git",
    "node_modules",
    "target",
    "__pycache__",
    ".venv",
    "vendor",
]
ignore_files = [
    "*.lock",
    "*.min.js",
    "*.min.css",
]

[retriever]
mode = "sqlite"
max_entry_bytes = 10485760
max_entries = 200
retention_days = 30
compression = true
tee_max_files = 20
tee_max_file_size = 1048576
tee_on_success = false

[telemetry]
enabled = false

[hooks]
exclude_commands = [
    "^git show [^ ]*:",
    "diff",
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
transparent_prefixes = []
suppress_hook_warning = true

[limits]
grep_max_results = 200
grep_max_per_file = 25
status_max_files = 15
status_max_untracked = 10
passthrough_max_chars = 2000

[awareness]
level = "default"
[exit 0]
[check: a [hooks] table without the key, new: rtk config exits 0 and reads the file: ok]
[check: a [hooks] table without the key, new: the effective config holds the four entries: ok]
[check: a [hooks] table without the key, new: the effective config keeps 'suppress_hook_warning = true': ok]
$ rtk hook check 'git show HEAD:x | tail -n 5'
No rewrite for: git show HEAD:x | tail -n 5
[exit 1]
[check: a [hooks] table without the key, new: the hook leaves the probe alone: ok]

## no [hooks] table: the old instruction adds the same block as the new one here; not repeated

## no [hooks] table, new instruction
### starting file
[tracking]
enabled = true
history_days = 30
### <scratch>/scratch-home/.config/rtk/config.toml after the new instruction
[tracking]
enabled = true
history_days = 30

[hooks]
exclude_commands = [
  "^git show [^ ]*:",
  "diff",
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
  '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
$ rtk config
Config: <scratch>/scratch-home/.config/rtk/config.toml

[tracking]
enabled = true
history_days = 30

[display]
colors = true
emoji = true
max_width = 120

[filters]
ignore_dirs = [
    ".git",
    "node_modules",
    "target",
    "__pycache__",
    ".venv",
    "vendor",
]
ignore_files = [
    "*.lock",
    "*.min.js",
    "*.min.css",
]

[retriever]
mode = "sqlite"
max_entry_bytes = 10485760
max_entries = 200
retention_days = 30
compression = true
tee_max_files = 20
tee_max_file_size = 1048576
tee_on_success = false

[telemetry]
enabled = false

[hooks]
exclude_commands = [
    "^git show [^ ]*:",
    "diff",
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*show\s+(?:[^\n]*\s)?[^\s]*:',
    '^git\s+(?:(?:-C|-c|--git-dir|--work-tree)\s+\S+\s+|--\S+\s+)*branch(?:\s|$)',
]
transparent_prefixes = []
suppress_hook_warning = false

[limits]
grep_max_results = 200
grep_max_per_file = 25
status_max_files = 15
status_max_untracked = 10
passthrough_max_chars = 2000

[awareness]
level = "default"
[exit 0]
[check: no [hooks] table, new: rtk config exits 0 and reads the file: ok]
[check: no [hooks] table, new: the effective config holds the four entries: ok]
[check: no [hooks] table, new: the effective config keeps 'history_days = 30': ok]
$ rtk hook check 'git show HEAD:x | tail -n 5'
No rewrite for: git show HEAD:x | tail -n 5
[exit 1]
[check: no [hooks] table, new: the hook leaves the probe alone: ok]

## files under <scratch>/scratch-home afterwards
['.config/rtk/config.toml']
[check: rtk wrote no file but the config: ok]

## result
PASS: all 23 checks held
