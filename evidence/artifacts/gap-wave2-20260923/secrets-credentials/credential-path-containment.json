{
  "schema_version": 1,
  "id": "credential-path-containment-20260923",
  "date": "2026-09-23",
  "source_revision": "92bb279a09877e7add057272babbfc78a4d4ef3f",
  "layer_ids": ["secrets-credentials"],
  "gap_refs": [
    {"layer_id": "secrets-credentials", "gap_index": 0}
  ],
  "purpose": "Gap 0 says the claim that native sign-ins and credentials stay off any checkout is source-review only (a policy value and decisions text), with no executed evidence. This check tests, path-only and without reading any credential file's contents, whether the actual native credential paths (Claude, Codex, gh, the TypeSafe env file) resolve outside both this catalog worktree and the agent-lab checkout, and whether git in each checkout would recognize or ignore credential-shaped filenames if one were ever placed there.",
  "preregistered": "[Fix-round caveat: this text was authored in the same working session as the commands below, after path resolution had already been inspected at least once, not strictly before it -- see PREREGISTRATION-NOTE.md and the matching limitations entry. It reads as fitted to the already-known .gitignore contents (see the specific-name predictions below), so it should be read as a documented expectation/method description, not a blind ex-ante prediction.] Expected: each of ~/.claude/.credentials.json, ~/.codex/auth.json, ~/.config/gh/hosts.yml, and ~/.config/typesafe/agent-lab.env resolves to an absolute path outside both ~/code/nas-wt-w2-secrets-credentials and ~/code/agent-lab (containment by path prefix, not content). `git ls-files` in each checkout is expected to return no credential-shaped tracked filename. `git check-ignore` for hypothetical same-named files at each checkout root is expected to show that only bare `.env`/`.env.*` names are covered by .gitignore; `.credentials.json`, `auth.json`, `hosts.yml`, and `agent-lab.env`-shaped names are NOT covered by any gitignore rule in either checkout, which would be recorded as a real (if currently moot, since the files are never placed inside) enforcement gap: containment today rests on the files never being written inside the checkout, not on a git-side backstop for those specific names. Broker credentials: the adaptive-paper market_research.py credentials() loader takes a required --env-file CLI argument with no default in-repo path, so containment there is a caller-supplied-path property, not a fixed path to test. No file contents are read or printed; only path resolution, `git ls-files`, and `git check-ignore` output are inspected.",
  "commands": [
    {
      "cmd": "stat -c '%a %n' ~/.claude/.credentials.json ~/.codex/auth.json ~/.config/gh/hosts.yml ~/.config/typesafe/agent-lab.env",
      "exit": 0,
      "duration_s": 0.02,
      "output_excerpt": "600 ~/.claude/.credentials.json\n600 ~/.codex/auth.json\n600 ~/.config/gh/hosts.yml\n600 ~/.config/typesafe/agent-lab.env",
      "output_sha256": "path-only-stat-not-hashed"
    },
    {
      "cmd": "python3 -c path-prefix-containment-check (see result)",
      "exit": 0,
      "duration_s": 0.01,
      "output_excerpt": "~/.claude/.credentials.json: outside both checkouts\n~/.codex/auth.json: outside both checkouts\n~/.config/gh/hosts.yml: outside both checkouts\n~/.config/typesafe/agent-lab.env: outside both checkouts",
      "output_sha256": "path-only-not-hashed"
    },
    {
      "cmd": "cd ~/code/nas-wt-w2-secrets-credentials && git ls-files | grep -iE '\\.credentials\\.json$|^auth\\.json$|hosts\\.yml$|agent-lab\\.env$|/\\.env$|\\.env\\.local$'",
      "exit": 1,
      "duration_s": 0.15,
      "output_excerpt": "(no output; exit 1 = grep found no match, i.e. no credential-shaped filename is tracked)",
      "output_sha256": "empty-grep-not-hashed"
    },
    {
      "cmd": "cd ~/code/nas-wt-w2-secrets-credentials && for f in .env .credentials.json auth.json agent-lab.env; do git check-ignore -v \"$f\" || echo \"$f: NOT ignored\"; done",
      "exit": 0,
      "duration_s": 0.05,
      "output_excerpt": ".gitignore:1:.env\t.env\n.credentials.json: NOT ignored\nauth.json: NOT ignored\nagent-lab.env: NOT ignored",
      "output_sha256": "check-ignore-catalog-not-hashed"
    },
    {
      "cmd": "cd ~/code/agent-lab && git ls-files | grep -iE '\\.credentials\\.json$|^auth\\.json$|hosts\\.yml$|agent-lab\\.env$|/\\.env$|\\.env\\.local$'",
      "exit": 1,
      "duration_s": 0.2,
      "output_excerpt": "(no output; exit 1 = no match)",
      "output_sha256": "empty-grep-not-hashed"
    },
    {
      "cmd": "cd ~/code/agent-lab && for f in .env .credentials.json auth.json agent-lab.env; do git check-ignore -v \"$f\" || echo \"$f: NOT ignored\"; done",
      "exit": 0,
      "duration_s": 0.05,
      "output_excerpt": ".gitignore:4:.env\t.env\n.credentials.json: NOT ignored\nauth.json: NOT ignored\nagent-lab.env: NOT ignored",
      "output_sha256": "check-ignore-agent-lab-not-hashed"
    },
    {
      "cmd": "grep -n 'env-file\\|env_file\\|def credentials' ~/code/nas-wt-w2-secrets-credentials/blueprints/us-equities/adaptive-paper/market_research.py",
      "exit": 0,
      "duration_s": 0.02,
      "output_excerpt": "355:def credentials(path):\n386:    parser.add_argument(\"--env-file\", type=Path, required=True)\n396:        key, secret = credentials(args.env_file)",
      "output_sha256": "broker-argparse-not-hashed"
    }
  ],
  "tool_versions": {"git": "2.43.0", "python3": "3.12"},
  "installs": [],
  "model_calls": [],
  "result": "Path-only containment holds for all four checked native credential paths: ~/.claude/.credentials.json (mode 600), ~/.codex/auth.json (mode 600), ~/.config/gh/hosts.yml (mode 600), and ~/.config/typesafe/agent-lab.env (mode 600) each resolve to an absolute path with neither ~/code/nas-wt-w2-secrets-credentials/ nor ~/code/agent-lab/ as a prefix, i.e. none is inside either checkout. `git ls-files` in both checkouts returns no tracked file matching any of these credential-shaped filenames. However, `git check-ignore` shows the containment is NOT backed by a git-side rule for these specific names in either checkout: only bare `.env`/`.env.*` is covered by .gitignore (catalog .gitignore:1-4, agent-lab .gitignore:4-6); a hypothetical `.credentials.json`, `auth.json`, or `agent-lab.env` placed at either checkout root would NOT be gitignored and could be accidentally `git add`ed. This is a real, previously unrecorded enforcement gap, though it is currently moot because the actual credential files are never written inside either checkout (containment today is a location fact, not a gitignore backstop). For the broker credential path (gap 0 also names 'broker env files'): market_research.py's credentials() loader takes a required `--env-file` argument with no default and no in-repo fallback path, so there is no fixed broker credential path to test for containment; containment there is a caller-supplied-path property enforced by argparse `required=True`, verified by source inspection of lines 355-396.",
  "settles_gap": "partially",
  "verdict_impact": {
    "could_change_verdict": false,
    "direction": "supports_incumbent",
    "explanation": "This is the first executed, path-only evidence (rather than source review of policy text alone) that the four checked native credential files are not inside either checkout and are not tracked by git. It advances gap 0 from 'source review only' to having one executed containment check, but does not settle it: (a) it covers only the current moment on this host, not that sign-ins 'stay off' the checkout over time or across a login/re-login cycle; (b) it surfaces a real gitignore-naming gap (credential-shaped filenames other than .env are not gitignored) that was not previously recorded, which is a genuine (if currently inert) weakening of the enforcement story rather than a strengthening of it; (c) the broker-credential path is a caller-supplied argument, not a fixed location, so 'stays off the checkout' there is an argparse-required-argument property, not a location fact like the other three."
  },
  "evidence_class": "native_proven",
  "limitations": [
    "Only a single point-in-time snapshot; does not show credentials stay off the checkout over time, across logins, or after a native re-authentication.",
    "Does not cover other hosts (Mac acceptance excluded, matching the existing gap text).",
    "File CONTENTS were never read or printed, per the instruction not to print credential file contents; only path, mode bits, and git tracking/ignore status were inspected.",
    "The .gitignore-naming gap found here (credential-shaped names other than .env/.env.* are not ignored) is a new, previously unrecorded observation; it does not itself cause exposure today because no such file exists inside either checkout, but it means a future accidental placement would not be caught by .gitignore alone.",
    "A guarded `gitleaks dir .` scan of the live worktree was not re-run in this receipt (gitleaks' per-user lock was held by a concurrent session's scan for the full duration of this unit); the existing evidence/artifacts/gap-resolution-20260922/secrets-credentials/gitleaks-repo-history-scan.json receipt already covers that angle (0 findings in dir mode) and is not duplicated here.",
    "Broker credential containment was verified by source inspection of the argparse contract (required=True, no default), not by an executed run with a real or synthetic broker credential file.",
    "FIX ROUND: this receipt's `preregistered` field was written after the underlying `git check-ignore`/`git ls-files` results were already known, not strictly before running them, and predicts the specific .gitignore-name outcome closely enough to read as fitted to that already-known result; see PREREGISTRATION-NOTE.md in this same directory. The underlying commands, exit codes, and output were not re-run or altered."
  ],
  "raw_artifacts": []
}
