Annotation to raw/3-live-loki-probe.txt (added 2026-09-23 in fix round 3, after the independent Opus review in raw/review-opus-round3.json; the raw file itself is left unedited).

The second block's key "signature_detection_demonstrated": true is a misnomer. live_session_probe.py sets it to
(live_api_request_events_in_window > 0): it only shows that the query path returned api_request events carrying
token fields (621 in the window). No positive control matched a known event in that run, so it is NOT evidence
of detection power. Read it as "query_path_returns_api_request_token_fields".

Detection power of the same matcher (live_session_probe.signature_hits, imported unchanged) was first shown in
fix round 3 by live_probe_fr3.py: the field values of a known live api_request event from the same window
matched 1 event, while the child's signature matched 0 (raw/3-fr3-live-probe.json, loki section).
live_session_probe.py is left unchanged so its committed hash still matches this raw output.
