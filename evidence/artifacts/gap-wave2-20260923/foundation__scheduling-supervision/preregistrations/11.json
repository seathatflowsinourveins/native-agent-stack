{
  "gap_index": 11,
  "written_at": "2026-09-22T22:42:40-04:00",
  "expectation": "One driver invocation executes both oracles: (a) the unchanged upstream Dagu retry tests at v2.16.6 (source archive sha256 8f4b1095..., `go test -count=1 -run '^TestRetryCommand($|_)' -timeout 12m -v ./internal/cmd`, Go 1.27.0 downloaded into the isolated cache, GOTOOLCHAIN=local) and (b) a real project workflow (validate.py + build_ecosystem.py --check on a HEAD snapshot) carried through an orderly-restart analog: boot 1 is an unprivileged user+pid+mount namespace whose init runs `dagu start`; when finalize waits, init receives SIGTERM, forwards it, and the namespace is torn down; boot 2 is a fresh namespace whose boot service checks the changed boot token and runs `dagu retry --run-id <same> --step finalize`. The disposable QEMU guest reboot itself cannot run here (driver refuses non-hosted runners; no qemu-system/cloud-image-utils without sudo; user not in the kvm group; pushing to dispatch the hosted workflow is forbidden).",
  "criteria": "Advanced iff both oracles pass in one run result: upstream tests exit 0 with 0 failures, and the real workflow shows aborted-by-shutdown -> retried succeeded under the same run id with checkpoint count 1 and outputs equal to a direct reference run. Cannot be settled here because the guest kernel reboot protocol is not reproduced; that remainder is named."
}
