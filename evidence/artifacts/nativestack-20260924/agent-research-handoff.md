# NativeStack offline research and Nautilus execution — 2026-09-24

Scope: agent-owned prefixes only, pinned catalog `v2026.09.23.1` / `40828dfea7bca910032e93b966bbdfdd729f9f44`; no broker, SEC network, model request, shared client config, live service, or other distro changed. Every `agent-research-*` command receipt under `work/receipts/` includes argv, streams, SHA-256, time and actual exit. Catalog checkout files remain unmodified.

| Component | Installed identity and prefix | Acceptance |
| --- | --- | --- |
| SDK | CPython 3.13.15, uv 0.12.17, 36 hash-locked distributions; `~/.local/share/codex-ecosystem/sdk-py313` | `agent-research-sdk-sync` exit 0 and `agent-research-sdk-check` exit 0. Full suite **failed**, below. |
| LEAN | Source `985ef30ad3ac774218c5ac516b4cb0aa2655730f`, three-line dependency patch plus 22 locked project graphs; `~/.local/share/codex-ecosystem/lean/Lean` | Microsoft .NET SDK 10.0.401 Linux x64 archive matched current official metadata SHA-512 `51c8b999…fbce25b`; locked restore exit 0; build exit 0, **7,730 warnings**, 0 errors; launcher native NuGet vulnerable list empty; unchanged default bundled backtest exit 0, 3 simulated orders, `OrderListHash f209ed42701b0419858e0100595b40c0`. No adapter or brokerage initialization. |
| skfolio | 1.2.9 plus 19 hash-locked distributions; `research-evaluation/venv` | `uv pip check` exit 0; source and nine LEAN inputs frozen and hash checked; 20 development folds, 6,605 ledger rows, reserved selection `momentum20`, inputs unchanged; 12 boundary tests passed. Price labels, not P&L. |
| EdgarTools | 5.58.0 plus 42 version-locked distributions; `catalyst-provenance/sdk` | `uv pip check` exit 0; offline two-member index fixture complete; pinned upstream 8-K fixture converted to 5,325 Markdown bytes with no SEC request and temporal quarantine retained; 21 provenance tests passed. Lock has versions, not artifact hashes. |
| NautilusTrader | 2.0.0rc5 / NumPy 2.5.3 / pandas 3.0.6 in `nautilus-py312`; Python 3.12.3, Bubblewrap 0.9.0-1ubuntu0.3 | Verified unchanged upstream quickstart SHA-256 `487e6807…cb2c53`. Corrected isolated runs `three` and `four` exited 0 with 10,000 bars, 1,804 events, 902 simulated fills, 451 flat positions, 903 account rows, cash `1,000,000.00→1,000,431.00`, realized PnL `431.00`, byte-identical account CSVs and native result. Only loopback inside distinct network namespace; `/home` absent; upstream disposal completed. `agent-research-nautilus-verify-corrected` exit 0. No broker connection or equity strategy claim. |
| Dagu | 2.16.6 publisher SHA-256 `06c3ed95…6c167717`; `dagu/2.16.6` and private `dagu/home` mode-0600 config | Accepted `agent-research-dagu-run2` exit 0: three steps succeeded, 6 LEAN events/3 orders, baseline validator 69 components/5,055 files/4 profiles/142 receipts passed, catalog validator 148 unique repositories. This uses an owned DAG copy with only timeout 180→900 seconds. No server or user unit started. |

**Retained failures and correction.** First skfolio venv attempt (`agent-research-eval-venv`) exited 2 because the interpreter was actually under uv's managed Python path, not guessed under ecosystem tools; corrected venv and study passed. First Nautilus verifier (`agent-research-nautilus-verify`) exited 1 because mounting the venv at its `~/...` path made `/home` exist inside Bubblewrap. Those initial engine runs had valid synthetic outputs but did not meet the stricter isolation criterion. Remounting the same venv at `/opt/nautilus` and using fresh run directories produced the accepted two runs. First Dagu execution (`agent-research-dagu-run`) timed out at its published 180 seconds after `summarize`; the copied DAG raised only `timeout_sec` to 900, and run2 succeeded. All failures remain in receipts.

**Full SDK suite limitation.** `agent-research-sdk-tests` actual exit **1**: 3,501 tests, 1 failure, 203 skips in 504.993 s. `test_adaptive_paper_sessions.SessionClockTests.test_year_outside_calendar_refused` expected `ValueError("session_calendar_out_of_range")` for a 2025 date, but no error was raised. The test comments explicitly assume optional `exchange_calendars` is absent; the selected SDK lock installs `exchange-calendars 4.13.2`, and `sessions.py` intentionally delegates out-of-2026 dates to that installed backend. This is an environment-assumption conflict in that one test, not evidence of a complete suite pass. No pinned source or test was patched; 203 skips are not passes. Focused skfolio and Edgar tests passed separately.

**Upstream drift checkpoint.** Current official Microsoft 10.0 release metadata still lists SDK 10.0.401 as latest SDK. [Nautilus releases](https://github.com/nautechsystems/nautilus_trader/releases) show 2.0.0rc5 at the reviewed commit. [Dagu releases](https://github.com/dagucloud/dagu/releases) now show 2.17.0 versus pinned 2.16.6; [PyPI skfolio](https://pypi.org/project/skfolio/1.3.0/) shows 1.3.0 versus pinned 1.2.9. The LEAN clone's `origin/master` at acquisition was `856327ff33869cba98393781000c4ab1d2af423d`, six commits after selected `985ef30a`. Hold pinned runtime for reproducibility and scope; recheck upgrades only after a reviewed dependency/API diff and fresh offline acceptance. [PyPI EdgarTools](https://pypi.org/project/edgartools/5.58.0/) lists 5.58.0 as current. No release or version was silently substituted.
